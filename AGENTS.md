# AGENTS.md — 아워골 최고 헌법 (The Supreme Constitution of OurGoal)
# (모든 AI 툴 공통 · 세부 규칙 완전 통합 불변 정본)

이 파일이 아워골 규칙의 유일한 최고 정본이다. 클로드 코드(CLAUDE.md)·안티그래비티(GEMINI.md, .agent/rules)·코덱스·커서·인간 개발자 모두 이 파일을 가장 먼저 읽고 따른다. 어떤 지침이나 규칙과 충돌 시에도 본 헌법이 배타적으로 우선한다. 본 헌법의 조문별 상세 적용 규칙, 코드 패턴, 금지 정규식, 검증 템플릿은 조문 안에 100% 통합되어 있으며, 본 헌법의 변경은 승인선 5(규범 변경)에 해당하여 상민님 승인 없이 수정할 수 없다.  
**헌법 버전**: 2026.09.15-SUPREME-INTEGRATED (상민님 직접 제정: 11대 조문 완전 통합본).  
**법령 전문**: `docs/rules/OURGOAL_ABSOLUTE_INTEGRITY_RULES.md`

---

## 제1조 (3대 본질 루프 및 인프라 헌법 - Essence & Infra)
1. **5대 축 필수 귀속**: 아워골의 모든 기능과 작업은 아래 5대 축 중 하나에 반드시 귀속되어야 한다:
   - **E1 (체크인 루프)**: 묻기 → 답하기 → 피드백 → 내 위치 확인 → 기록 (성취감이 동력).
   - **E2 (기록 회고)**: 오늘·일주일·한 달·1년·설정 기간 5단위 회고 (성취감·기록의 가치·더 나은 기록 의지 고취).
   - **E3 (동류 소통)**: 비슷한 목표/관심사 동류의 기록·목표·주간 피드·템플릿 확인 및 소통 ("내 인생에 실질적으로 유익하다" 실질 체감).
   - **INFRA (기반 인프라)**: 무결성·품질·배포·테스트 기반 (본질이 서 있을 기술적 땅).
   - **FIX (버그 수정)**: 기존 본질 기능의 결함 및 오작동 수정.
2. **판정 기준 및 가짜 가설 엄금**: 판정 기준은 "기능 존재"가 아니라 **"사용자가 유익함과 성취감을 실질적으로 느끼는가"**로만 판정한다. AI가 인프라 작업을 하면서 가짜 체감 가설을 날조하는 행위는 엄격히 금지하며, 순수 기술 작업은 정당하게 `[INFRA]` 축의 '품질·안정성 전제 가설'로 명시한다.
3. **G0 착수 티켓 필수 3요소**:
   - ① 본질 축 (E1/E2/E3/INFRA/FIX)
   - ② 체감 가설 ("어떤 사용자가, 어느 화면에서, 무엇을 느끼는가")
   - ③ 실데이터 근거 (로그·코드·사용자 발언 중 하나)
4. **유지보수 카탈로그 및 기본값 전진**:
   - 번들러 설정, 린터/포맷터, 라이브러리 보안 패치, JSDoc 주석, 빌드 툴체인 등 **5대 표준 유지보수 카탈로그는 상민님께 질문 없이 `[기본값: INFRA]`로 즉시 착수**한다.
   - 축 귀속이 모호한 신규 작업이라도 작업을 멈추지 않고 **`[기본값: 최유력 축]`으로 티켓을 열고 로컬 작업을 계속 전진**시키며 사후 보고한다.

---

## 제2조 (2중 8원칙 및 핫픽스 헌법 - Zero-Omission & Fast-Track)
1. **2중 8원칙 프로세스 의무**: 코드를 작성하기 전, 반드시 문제해결 8원칙을 2단계 연속 적용하여 문서를 수립해야 한다:
   - **1차 8원칙 (요구사항 정의서 REQ)**: `docs/specs/REQ-<기능명>.md`
     * ①문제 정확히 파악 ②본질·원인·핵심 파악 ③해결방식(FR-xx 요구사항 목록, 전수 인터랙션 목록, 데이터 보존 명세) ④1~3 재검토·보완 ⑤화면 간 연동 및 권한 절차 정리 ⑥절차 재검증 ⑦단계별 실행 기준 ⑧막히는 지점 사전 도출.
   - **2차 8원칙 (엔지니어링 작업계획서 PLAN)**: `docs/specs/PLAN-<기능명>.md` 및 `.Codex/작업계획서/<세션id 8자>.md`
     * ①파악 ②중심 배선(Wire) 식별 ③파일별 Before/After 및 변경 예산 ④재검토 ⑤구현 상세 순서 ⑥5대 무결성 검증 시나리오 ⑦체크리스트 ⑧블로커 대책.
2. **무누락·무축약(Zero-Omission) 규격**: AI 에이전트는 코드 작성 시 `// ... rest of code unchanged ...`, `/* 기존 코드 동일 */`, `// TODO: 나중에 구현`, `// FIXME` 등의 축약 주석을 남길 수 없으며, 100% 완전한 실행 코드로 작성해야 한다.
3. **10줄 이하 핫픽스 패스트트랙**:
   - 오타 수정, 패딩/마진 미세 조정, 단순 null 가드 등 **10줄 이하의 단순 버그 수정은 REQ/PLAN 대신 아래 `QUICK-FIX` 4줄 간이 서식으로 즉각 커밋**할 수 있다:
     ```markdown
     # QUICK-FIX: [수정 대상 요약]
     - 문제: (발생한 버그 또는 오타 1줄)
     - 원인: (버그 발생 원인 1줄)
     - 수정: (수정한 파일 및 코드 1줄)
     - 검증: (npm test 통과 및 실화면 확인 결과 1줄)
     ```
4. **핫픽스 안전핀 및 살라미 분할 방지**:
   - 단순 변수명(`user.name`)은 허용하되, **오직 '인증/권한 변경 함수(`supabase.auth.*`, RLS 정책)' 수정 시에는 줄 수와 무관하게 패스트트랙이 전면 금지**된다.
   - 동일 파일 또는 동일 함수를 1시간 내 3회 연속 분할 수정할 경우 기계적 게이트가 작동하여 정식 REQ/PLAN 전환을 상민님께 질문한다.

---

## 제3조 (껍데기 UI 방지 4위 1체 배선 및 격리 헌법 - 4-in-1 Wiring)
1. **4위 1체 배선 의무**: 모든 인터랙티브 UI 요소(버튼, 링크, 입력창 등)는 반드시 아래 4대 요소가 한 세트로 완결되어야 한다:
   - ① **마크업(HTML)**: 고유 ID, 접근성 라벨, 올바른 시맨틱 태그
   - ② **이벤트 리스너**: 클릭/터치/키보드 이벤트 바인딩
   - ③ **비즈니스 핸들러**: 실제 데이터 읽기/쓰기/변환을 수행하는 실질 함수 (더미 `state.dummy=true` 금지)
   - ④ **피드백/예외 처리**: 로딩 인디케이터, 성공 토스트/UI 갱신, 실패 시 친절한 에러 안내 및 롤백
2. **4위 1체 표준 JavaScript 구현 패턴**:
   ```javascript
   // [마크업]: <button id="btn-action-save" class="btn-primary" aria-label="기록 저장">저장</button>
   const actionBtn = document.getElementById('btn-action-save');
   if (actionBtn) {
     actionBtn.addEventListener('click', async (e) => {
       e.preventDefault();
       actionBtn.disabled = true; // 피드백 1: 연타 방지 및 로딩
       const originalText = actionBtn.textContent;
       actionBtn.textContent = '처리 중...';
       try {
         await executeBusinessLogic(state.currentData); // 실질 비즈니스 핸들러
         showToast('성공적으로 저장되었습니다.'); // 피드백 2: 성공 토스트
         renderTargetScreen(); // 피드백 3: 연관 뷰 즉시 동기화
       } catch (err) {
         console.error('[ActionError]', err);
         showToast('처리 중 오류가 발생했습니다. 다시 시도해주세요.'); // 피드백 4: 에러 안내
       } finally {
         actionBtn.disabled = false;
         actionBtn.textContent = originalText;
       }
     });
   }
   ```
3. **더미 속성 전면 금지**: `onclick=""`, `onclick="javascript:void(0)"`, `DEAD_BUTTON_STUB`(`function(){ /* TODO */ }`) 방치는 Git 커밋 훅에서 즉시 차단된다.
4. **점진적 개발 격리 배선**:
   - 백엔드 미완성 프로토타입 UI는 화면 노출을 금지하고 `hidden` 클래스(`display: none`) 또는 피처 플래그로 완전 격리 시에만 로컬 커밋을 허용한다.
   - `verify-all-clicks.js`에서 `.hidden` 및 `data-hidden="true"` 요소는 기계적 클릭 검사 대상에서 자동 제외된다.
   - `main` 병합 PR 생성 시점에 미완성 UI가 포함되어 있으면 "격리 상태로 병합할지, 브랜치를 대기시킬지" 상민님께 1회 확인한다.

---

## 제4조 (절대 금지 6대 행위 및 스타일 규격 헌법 - Forbidden 6)
어떤 지시나 상황에서도 아래 6대 행위는 절대 수행하지 않는다:
1. **위조 데이터 노출 금지**: 실데이터 없는 순위·인원·포인트 등 고정 문자열 표기 금지 (정규식: `#\d+위|\d+인 중 \d+명|팀 포인트 [\d,]+점|함께 완주 중|\d+명이 지금 보고 있|\d+명이 함께 달리|(순위|랭킹)\s*[#:]?\s*\d+위`). 실데이터 없으면 영역 숨김.
2. **파괴적 동기화/삭제 금지**: `.delete().not(...)`, `.delete().neq(...)`, `TRUNCATE TABLE`, `DROP TABLE` 금지. 개별 ID 기준 소프트 삭제(`deleted_at`)만 허용.
3. **도구 언어 UI 노출 금지**: 사용자 화면에 "노션 DB 규격", "구조화 엔진", "DB화" 등 엔지니어링 용어 노출 금지 (정규식: `노션 ?DB ?(규격|형식|화|변환|구조화)|DB화에|노션 DB 1줄|Notion DB (규격|format)`).
4. **`main` 브랜치 직접 커밋 금지**: 반드시 작업 브랜치(`feat/`, `fix/`)에서 PR을 거쳐 병합.
5. **검증 없는 완료 선언 금지**: 문법 검사, 스모크 테스트, 실화면 확인 없이 "완료" 표기 금지 (확인 못 했으면 "미확인" 명시).
6. **아이디어 즉시 구현 금지**: 작업 중 떠오른 새 기능은 구현하지 않고 노션 작업대기목록에 '제안'으로 적재.
- **스타일 가이드 예외 배선**: `ui.css`에 정의된 커서 모양(`cursor: pointer`), 마진, 패딩, 폰트 불일치 보정은 새 아이디어가 아닌 `[FIX]`로 자동 인정하여 즉시 수정을 허용한다.

---

## 제5조 (승인선 8대 통제 및 자동 커밋 분할 헌법 - Approval Lines & Auto-Split)
1. **승인선 8대 감지 키워드 (감지 시 즉시 중단 후 `[결심 필요]` 보고)**:
   - ① **돈**: 결제, 구독, 요금제, 토스페이먼츠 (`tosspayments|paywall|subscription|billingKey|price|가격|결제`)
   - ② **개인정보 확대**: 전화번호, 생년월일, 주민번호, 주소, GPS 위치, 연락처 (`phone|birth|생년월일|주민|address|gps|geolocation|contacts`)
   - ③ **기존 기능 삭제**: 40줄 이상 기능 코드 삭제 또는 핵심 인터랙션 제거
   - ④ **되돌릴 수 없는 바깥 행위**: 실서버 배포, Google Play 제출, 푸시 발송, 외부 전송 (`vercel --prod|gh release|play-console|sendgrid|web-push\.sendNotification|smtp|DROP\s+TABLE`)
   - ⑤ **규범 변경**: `AGENTS.md`, `rules.json`, `docs/rules/*`, 훅/게이트 파일 수정
   - ⑥ **본질 무관 기능**: 본질 축(E1~E3)이나 체감 가설이 없는 feat 커밋
   - ⑦ **위조 숫자 표시**: 실데이터 없는 사회적 증거용 숫자 표기 시도
   - ⑧ **대량 변경**: 단일 커밋 500줄 초과 또는 `index.html` 순증가 300줄 초과
2. **테스트/문서 라인 한도 제외**: `test/`, `sim/`, `scripts/`, `docs/` 파일은 500줄 한도 계산에서 완전 제외한다 (오직 프로덕션 런타임 코드만 적용).
3. **3계층 자동 분할 커밋(Auto-Split)**: 프로덕션 코드가 500줄을 초과할 때 작업을 중단하지 않고, AI가 아래 3계층으로 자동 분할 커밋하여 통과시킨다:
   - `[태그] #티켓 feat(db): 스키마·타입·상태머신 정의`
   - `[태그] #티켓 feat(logic): 비즈니스 로직 및 데이터 핸들러`
   - `[태그] #티켓 feat(ui): UI 마크업·CSS 및 이벤트 리스너 배선`

---

## 제6조 (1티켓 1PR 및 피처 플래그 헌법 - Single Ticket & Feature Flag)
1. **1 PR = 1 티켓 엄수**: 여러 티켓을 하나의 PR에 묶지 않으며, 브랜치명은 `feat/<날짜>-<설명>` 또는 `fix/<날짜>-<설명>`을 사용한다.
2. **커밋 메시지 표준 규격**: `[태그] #티켓번호 feat/fix/chore: 요약` (태그: E1, E2, E3, INFRA, FIX).
3. **PR 설명 4블록 표준 템플릿**:
   ```markdown
   ## 1. 문제 및 본질 (Why)
   - 티켓 번호 및 본질 축 (E1/E2/E3/INFRA/FIX):
   - 사용자 체감 가설:
   ## 2. 해결 방식 및 타당성 (What & How)
   - 주요 변경 사항 및 설계 타당성:
   - 4위 1체 배선 내역:
   ## 3. 구현 및 검증 (Verification)
   - npm test 결과 (238개 스모크 및 무결성 게이트):
   - 실기기/로컬 수동 확인 내용:
   ## 4. 재검증 및 연계 (Re-verification)
   - 10종 페르소나 데이터 무손실 검증 및 화면 간 상태 전파 확인:
   ```
4. **피처 플래그 트렁크 개발 배선**: 며칠씩 고립되어 머지 충돌을 유발하는 거대 에픽 브랜치를 금지하고, `window.__FEATURE_FLAGS__.FEATURE_NAME = false` 상태로 1티켓 단위로 로컬 메인에 매일 순차 병합하여 상민님이 진행률을 실시간 체감할 수 있게 한다.

---

## 제7조 (5대 무결성 전수 검증 및 스크립트 헌법 - Zero-Defect Suite)
모든 코드는 머지 전 아래 5대 무결성 검증을 전수 통과해야 한다 (`npm test`):
1. **제1검증 (전수 클릭/터치)**: 앱 내 모든 인터랙티브 요소 실제 동작 확인 (데드클릭 0건, 런타임 에러 0건).
2. **제2검증 (전 기능 & 전 UX 회귀)**: 계정/세션 관리(게스트/소셜 로그인), 온보딩, 3대 본질 루프(E1 체크인, E2 회고, E3 동류소통) 핵심 로직 보존.
3. **제3검증 (유저 데이터 100% 무손실 보존)**: 10종 가상 페르소나 데이터 통과 시뮬레이션:
   - **아바타**: 커스텀 제작 이미지, 특징 분석값, 잔여 제작 횟수, 착용 상태
   - **목표**: 생성 목록, 진행률, 마일스톤, 세부 할 일, D-Day
   - **기록**: 과거 체크인 전수, AI 피드백, 스트릭, 히트맵
   - **화면 세팅**: 테마, 카드 접힘/펼침, 필터/정렬 옵션
   - `assert.deepStrictEqual` 딥 이퀄 대조 0바이트 손실 입증.
4. **제4검증 (화면 간 상태 동시 전파)**: 한 화면 데이터 변경 시 연관 뷰(홈/기록/통계/캘린더) 실시간 동시 갱신.
5. **제5검증 (기술 규격 100% 통과)**: `npm test` 238개+ 스모크 테스트 0 실패 유지.
6. **치팅 방지 배선**: AI의 픽스처 JSON 손수정 전면 금지. 반드시 `scripts/migrate-fixture.js` 스크립트를 작성하여 구 픽스처 → 신 픽스처 데이터 변환을 입증하고 "데이터 손실 0건 입증 요약문" 보고.

---

## 제8조 (직관적 작업 상태 6단계 보고 및 프리뷰 배포 헌법 - Status & Preview)
1. **추상적 개발자 언어 전면 금지**: "반영했습니다", "검증 완결" 등 모호한 표현 전면 금지.
2. **직관적 6단계 명칭 및 물리적 구동 의미 명시 의무**:
   - **[1단계: 기획·설계 상태]**: 코드는 아직 작성하지 않았고, 작업계획서와 규격(REQ/PLAN) 문서만 작성된 상태.
   - **[2단계: 내부 시뮬레이션 상태]**: 작업 브랜치에서 코드 작성 및 단위/스모크/무결성 테스트 통과 상태 (앱 실제 구동 전).
   - **[3단계: 로컬 수동 확인 상태]**: 개발자의 로컬 환경(`localhost:8000` 또는 브라우저)에서 화면을 직접 띄워 눈으로 보고 누른 상태.
   - **[4단계: 로컬 메인 병합 상태]**: 작업 브랜치가 로컬 `main`(정본)에 합쳐져 로컬 앱에서 실제로 동작하는 상태 (원격 push 전).
   - **[5단계: 실서버 배포 상태]**: GitHub로 `git push`되어 Vercel 실서버 배포가 완료되어 실제 웹사이트에서 서비스되는 상태.
   - **[6단계: 실운영 최종 확인 상태]**: 실제 운영 주소(`ourgoal-app.vercel.app`)에 직접 접속해 라이브 환경에서 실동작까지 최종 확인한 상태.
3. **프리뷰 자동 배포 및 승인 분리 배선**:
   - 일반 세션의 정상 마감점은 **[4단계: 로컬 메인 병합 상태]**이다.
   - 4단계 완료 즉시 질문 없이 **'Vercel 프리뷰 배포(5A단계)'를 자동 실행**하여 실기기 테스트용 임시 URL(`ourgoal-preview-xxx.vercel.app`)을 상민님께 제공한다.
   - 상민님의 명시적 승인("배포" 또는 "1")을 받은 직후에만 5단계(프로덕션 실배포) 및 6단계(실운영 최종 확인)로 진입하여 완결한다.

---

## 제9조 (용어 헌법: '히트맵' 강제 및 글로벌 I/O 헌법 - Terminology)
1. **금지 단어 사용 전면 영구 금지**: 코드, 주석, UI 화면, 안내 문구, 기획서, 회고, 커밋 메시지 등 앱 내 모든 영역에서 해당 금지 단어의 사용을 전면 영구 금지한다.
2. **단일 용어 표기**: 모든 연속성 시각화는 예외 없이 **'히트맵(Heatmap)'**으로만 명명하고 표기한다.
3. **글로벌 I/O 자동 치환 배선**: 외부 라이브러리(GitHub API 등)에서 유입되는 데이터는 네트워크 진입점(`api/fetch-wrapper.js`)에서 글로벌 자동 치환기가 '히트맵'으로 키를 자동 매핑 처리하여, 코드베이스 내 오염 및 개별 번역 질문을 원천 제거한다.

---

## 제10조 (Tri-Sync 3자 상호 동기화 및 미디어 통제 헌법 - Tri-Sync & Media Control)
1. **3자 상호 동기화 원칙**: 모든 작업의 REQ, PLAN, 실행 결과, DB 스키마는 **Notion(중앙 DB) - Obsidian(로컬 지식) - Command Center(HUD/저널)** 3곳에 즉시 상호 동기화된다.
2. **집행 및 검증 절차**:
   - 데이터 변경 시: `node C:/dev/command-center/lib/tri-sync.js sync <파일|작업ID>`
   - 세션 종료 전: `node C:/dev/command-center/lib/tri-sync.js check`로 100% 무결성 검증 (rate: 100% 필수).
3. **미디어 자동생성 전면 차단**: 오직 **아워골 아바타 이미지 제작만 허용**하며, 로컬 ComfyUI, 릴스/쇼츠/카드뉴스 무인 합성기(`AI콘텐츠_자동화공장/08_제작_스크립트`) 및 `content-pipeline` 에이전트는 비활성화(Blocked) 상태를 유지한다.
4. **3-Way 자동 중재 배선**: 노션-옵시디언 간 충돌 시, 3-Way 자동 중재 알고리즘이 서로 다른 섹션은 자동 병합하고, 문장 충돌 시 최근 5분 내 수정한 쪽을 임시 우선 적용 후 백업본(`.conflict-backup`)을 보존하며 상민님께 "자동 중재 완료 (1클릭 원복 가능)"으로 보고한다.

---

## 제11조 (지시 의도 스코프 및 단계별 멈춤 헌법 - Intent Scope & Boundary Control)
1. **4대 지시 모드 및 물리적 행동 한계선 절대 준수**:
   - **모드 1 [탐색·질문 모드]**: *"~할 수 있지?"*, *"~어때?"*, *"~가능한가?"*, *"~알려줘"* 등 질문형 어미 포함 시 **코드/파일/설정 수정을 전면 금지**하며, 오직 타당성 분석과 답변만 보고하고 대기한다.
   - **모드 2 [구상·설계 한정 모드]**: *"일단 구상만"*, *"초안만"*, *"적용하지는 말고"*, *"아이디어만"* 등 한정 지시 시 **실제 저장소 파일 수정을 전면 금지**하며, 텍스트 기획/설계안만 제시하고 대기한다.
   - **모드 3 [단계별 명시 정지 모드]**: *"~단계까지만"*, *"REQ까지만"*, *"작업계획서만 세우고 멈춰"*, *"이해했는지 요약만"* 등 단계 한정 시 **다음 단계 자동 진입을 금지**하고 해당 단계 완료물 보고 후 대기한다.
   - **모드 4 [전권 실행 모드]**: 상민님의 명시적 실행 명령(*"구현해"*, *"고쳐"*, *"진행"*, *"승인"*, *"1"*, *"적용해"*)이 내려진 경우에만 본 헌법에 따른 무중단 자율 실행이 발동된다.
2. **의문형 문장 처리 원칙**: 상민님의 문장에 의문형 어미가 포함되어 있으면 AI는 이를 전권 실행 명령으로 자의적 확대 해석할 수 없으며, 반드시 실행 여부를 확인한 뒤 명시적 승인을 받아야만 코드 수정에 착수할 수 있다.
3. **애매할 때 상민님 질문 패킷 규격**:
   ```markdown
   [결심 필요: 제N조 애매함 판정 요청]
   - 상황 요약: (무엇을 하려다 어떤 헌법적 경계선에 걸렸는지 1줄)
   - 애매한 쟁점: (왜 AI 혼자 결정하면 위험한지 핵심 이유 1줄)
   - 권장 선택지: [선택 1] (가장 안전하고 헌법에 부합하는 경로)
     * 선택 1: (진행 시 장점 및 결과 1줄)
     * 선택 2: (대안 경로의 장점 및 리스크 1줄)
   - 상민님 한 마디 응답: "1" 또는 "2"
   ```

---

## 제13조 (외부 연동 종단간 무결성 헌법 - E2E Integration)
1. **외부 앱 의존 껍데기 링크 전면 금지 및 인앱 완결**: 푸터, 서브메뉴, 모달 내 일체의 `mailto:` 등 외부 메일 클라이언트 의존 링크를 전면 영구 금지하고, 모든 고객 접수 창구는 인앱 모달로 단일화한다.
2. **클라우드 환경변수 3자 사전 동기화**: 로컬 `.env`에 존재하는 필수 토큰/키는 5단계 실서버 배포 전 반드시 프로덕션 클라우드(Vercel, Supabase)에 기계적으로 사전 동기화(`vercel env ls`)해야 하며, 침묵형 실패(Silent Failure)를 엄격히 금지한다.
3. **PWA·브라우저 캐시 강제 무효화**: 프론트엔드 핵심 코드 변경 시 반드시 `sw.js`의 `CACHE_NAME`을 당일/티켓 버전으로 즉시 갱신한다.
4. **6단계 실운영 최종 확인의 종단간(E2E) 물리적 실측**: 메인 URL 접속 확인만으로 6단계 완료 보고를 절대 금지하며, 실제 라이브 엔드포인트에 테스트 요청을 전송하여 최종 수신처(노션 DB 행 생성, 텔레그램 알림 도착)에 물리적으로 도달했음을 확인하는 '2중 물리적 증거(Evidence)'를 필수로 제출해야 한다.

---

## 제14조 (헌법 독점주의 및 법체계 위계 단일화 헌법 - Constitutional Exclusivity)
1. **법체계 위계 단일화**: 모든 규범과 규칙은 반드시 **[조(Article) ➔ 항(Paragraph) ➔ 호(Item) ➔ 목(Sub-item)]** 4단계 표준으로만 표기한다. 헌법의 대원칙이 '조'이며, 세부 법조문은 모두 '항'이다. 조 번호의 중복·난립을 영구 금지한다.
2. **헌법 독점주의 (사설 규칙 제정 전면 금지)**: 전 시스템에서 유효한 모든 개발·운영·협업 규범은 **오직 최고 헌법 정본(`OURGOAL_ABSOLUTE_INTEGRITY_RULES.md`) 안에 직접 편입·등재된 조문과 항만을 인정**한다. 헌법 밖에서 독자적으로 작성된 어떠한 사설 규칙 문서, 가이드라인, 관행도 법적 효력을 갖지 못하며 전면 무효다.
3. **신규 규칙 제정의 헌법 편입 의무**: 향후 프로젝트 진행 중 새로운 규칙, 기술 제약, 개발 가이드라인이 필요한 경우, 별도의 마크다운 파일로 분산시키지 않고 **반드시 최고 헌법에 신규 '조'를 신설하거나 기존 조문의 '항'으로 증설하여 상민님의 명시적 승인(승인선 ⑤: 규범 변경)을 거쳐서만 제정**할 수 있다.
4. **규범 변경의 절대 승인선 엄수**: 본 헌법의 조/항을 신설·수정·삭제하는 모든 행위는 상민님의 명시적 승인 없이는 단 1글자도 임의로 변경할 수 없다.

---

## 부칙: 기계적 집행 장치 및 버전
1. `scripts/verify-integrity-gate.js`: 껍데기 버튼 정적 검사, 10종 페르소나 데이터 무손실 딥이퀄 검증.
2. `scripts/essence-gate.js`: 커밋 태그, 금지 패턴, 승인선 8대, 줄 수 한도 검사.
3. `npm test`: 238개 이상 스모크 테스트 전수 통과.
4. `gate.js` (C:\dev\command-center\gate\gate.js): 상민님 공통 지침 및 게이트 훅.