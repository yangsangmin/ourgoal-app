# AGENTS.md — 아워골 최고 헌법 (The Supreme Constitution of OurGoal)
# (모든 AI 툴 공통 · 14대 조문 및 [조 ➔ 항 ➔ 호 ➔ 목] 단일 위계 완전 통합 불변 정본)

이 파일이 아워골 규칙의 유일한 최고 정본이다. 클로드 코드(CLAUDE.md)·안티그래비티(GEMINI.md, .agent/rules)·코덱스·커서·인간 개발자 모두 이 파일을 가장 먼저 읽고 따른다. 어떤 지침이나 규칙과 충돌 시에도 본 헌법이 배타적으로 우선한다. 본 헌법의 조문별 상세 적용 규칙, 코드 패턴, 금지 정규식, 검증 템플릿은 조문 안에 100% 통합되어 있으며, 본 헌법의 변경은 승인선 5(규범 변경)에 해당하여 상민님 승인 없이 수정할 수 없다.  
**헌법 버전**: 2026.09.16-SUPREME-14-ARTICLES (상민님 직접 제정: 14대 조문 단일 위계 완전 통합본).  
**법령 전문**: `docs/rules/OURGOAL_ABSOLUTE_INTEGRITY_RULES.md`

---

## 제1조 (3대 본질 루프 및 인프라 헌법 - Essence & Infra)
1. **제1항 [헌법의 목적 및 무결성 표준]**: 기능 추가, 변경, 백엔드/DB 변경 시 고질적 결함(버튼 먹통, 껍데기 버튼, 기능 고립, 데이터 유실, 상태 불일치, 코드 축약)을 원천 방지한다.
2. **제2항 [적용 범위]**: `ourgoal-app` 코드베이스 전 영역 및 DB 스키마 일체에 적용된다.
3. **제3항 [3대 본질 루프 및 5대 축 귀속 의무]**:
   - **E1 (체크인 루프)**: 묻기 → 답하기 → 피드백 → 내 위치 확인 → 기록 (성취감이 동력).
   - **E2 (기록 회고)**: 오늘·일주일·한 달·1년·설정 기간 5단위 회고 (성취감·기록의 가치 고취).
   - **E3 (동류 소통)**: 비슷한 목표/관심사 동류의 기록·목표·주간 피드·템플릿 확인 및 소통.
   - **INFRA (기반 인프라)**: 무결성·품질·배포·테스트 기반 (`[기본값: INFRA]`로 무중단 직진).
   - **FIX (버그 수정)**: 기존 본질 기능의 결함 및 오작동 수정.
4. **제4항 [6대 고질병 원천 근절 선언]**: 되던 기능 먹통(Regression), 껍데기 버튼(Dead Click), 상호연계 고립, 유저 데이터 유실(Data Loss), 화면 간 상태 불일치, 가짜 실제구현(Fake Implementation) 전면 근절.

---

## 제2조 (2중 8원칙 및 핫픽스 헌법 - Zero-Omission & Fast-Track)
1. **제1항 [1단계: 문제해결 8원칙 요구사항 정의서(REQ)]**: 코드를 작성하기 전, 반드시 `docs/specs/REQ-<기능명>.md`를 작성한다.
2. **제2항 [2단계: 문제해결 8원칙 작업계획서(PLAN)]**: `docs/specs/PLAN-<기능명>.md` 및 `.Codex/작업계획서/<세션id 8자>.md`를 작성한다.
3. **제3항 [10줄 이하 핫픽스 패스트트랙]**: 10줄 이하 단순 버그 수정은 REQ/PLAN 대신 `QUICK-FIX` 4줄 간이 서식(문제-원인-수정-검증)으로 즉각 커밋할 수 있다. (단, 인증/권한 변경 함수는 패스트트랙 전면 금지).

---

## 제3조 (껍데기 UI 및 가짜 실제구현 방지 4위 1체 배선 헌법 - 4-in-1 Wiring)
1. **제1항 [AI 코드 축약 및 생략 전면 영구 금지]**: `// ... rest of code unchanged ...`, `// TODO` 등 축약 주석을 영구 금지하며, 100% 완전한 실행 코드로 작성해야 한다.
2. **제2항 [4위 1체 배선 의무]**: 모든 UI는 [①마크업(HTML) + ②이벤트 리스너 + ③비즈니스 핸들러(실 사용자 계정 상호 연동) + ④피드백/예외 처리] 4위 1체로 완결되어야 한다.
3. **제3항 [미완성 UI 피처 플래그 격리 의무]**: 백엔드 미완성 프로토타입 UI는 화면 노출을 금지하고 `hidden` 클래스 또는 피처 플래그로 완전 격리 시에만 로컬 커밋을 허용한다.
4. **제4항 [외부 앱 의존 껍데기 링크 전면 금지]**: 앱 내 버튼 및 링크에서 `mailto:` 등 기기 외부 이메일 앱이나 외부 클라이언트를 강제 호출하는 의존성 링크를 전면 금지한다.

---

## 제4조 (절대 금지 7대 행위 및 스타일 규격 헌법 - Forbidden 7)
1. **제1항 [절대 금지 7대 행위]**:
   - 1호: **위조 숫자 날조 금지** (실데이터 없는 순위·인원·더미 카운트 표기 금지)
   - 2호: **파괴적 데이터 삭제 금지** (`.delete().not()`, `TRUNCATE TABLE`, 조건 없는 `DELETE` 금지)
   - 3호: **도구 언어 UI 노출 금지** (사용자 화면에 "노션 DB 규격", "DB화" 등 엔지니어링 용어 노출 금지)
   - 4호: **`main` 브랜치 직접 커밋 및 승인 없는 머지/푸시 금지**
   - 5호: **기계적 검증 없는 완료 보고 금지** (`npm test`, 스모크 테스트 통과 필수)
   - 6호: **아이디어 즉시 구현 금지** (작업 중 떠오른 새 기능은 노션 작업대기목록에 '제안'으로 적재)
   - 7호: **가짜 실제구현 전면 금지** (상대방 계정과 연동 없는 가상 봇 눈속임 영구 금지)
2. **제2항 [스타일 및 디자인 컴포넌트 일관성 규격]**: `ui.css` 전역 디자인 토큰 엄격 계승.

---

## 제5조 (승인선 8대 통제 및 자동 커밋 분할 헌법 - Approval Lines & Auto-Split)
1. **제1항 [5대 필수 승인선 통제 (감지 시 즉시 중단 후 `[결심 필요]` 보고)]**:
   - 1호: **돈** (결제, 구독, 요금제, 유료 API 호출 증설)
   - 2호: **개인정보** (전화번호, 주민번호, GPS, 보안 정책 변경)
   - 3호: **기존 기능 삭제** (40줄 이상 기능 코드 삭제 또는 핵심 인터랙션 제거)
   - 4호: **되돌릴 수 없는 바깥 행위** (원격 main PR 머지, 실서버 배포, Webhook/이메일 실발송)
   - 5호: **규범 변경** (본 헌법 조/항 개정, `AGENTS.md` 지침 변경)
2. **제2항 [추가 승인선 통제]**: 본질 무관 기능, 위조 숫자 날조, 3개 이상 도메인 대규모 아키텍처 개편.
3. **제3항 [500줄 초과 시 3계층 자동 분할 커밋]**: 프로덕션 코드가 500줄을 초과할 때 작업을 중단하지 않고 3계층(①DB ➔ ②Logic ➔ ③UI)으로 자동 분할 커밋(Auto-Split)하여 점진적으로 병합 통과한다.

---

## 제6조 (1티켓 1PR 및 피처 플래그 헌법 - Single Ticket & Feature Flag)
1. **제1항 [1 PR = 1 Ticket 원칙 및 4블록 설명 규격]**: 1 티켓 = 1 PR 엄수, 4블록(문제·해결·검증·재검증) 설명문 작성.
2. **제2항 [에픽 브랜치 격리 금지 및 일일 순차 병합]**: 피처 플래그 비활성화 상태로 1티켓 단위 로컬 메인에 매일 순차 병합.

---

## 제7조 (5대 무결성 전수 검증 및 스크립트 헌법 - Zero-Defect Suite)
모든 코드는 머지 전 아래 5대 무결성 검증을 전수 통과해야 한다 (`npm test`):
1. **제1항 [제1검증: Zero Dead-Click]**: 앱 내 모든 인터랙티브 요소 실제 동작 확인 (데드클릭 0건).
2. **제2항 [제2검증: Zero UX Regression]**: 계정/세션 관리, 온보딩, 3대 본질 루프 핵심 로직 보존.
3. **제3항 [제3검증: Zero Data Loss]**: 10종 페르소나 데이터(아바타, 목표, 기록, 화면세팅) 무손실 딥이퀄 대조 (0바이트 손실 입증).
4. **제4항 [제4검증: Full State Propagation]**: 한 화면 데이터 변경 시 연관 뷰(홈/기록/통계/캘린더) 실시간 동시 전파.
5. **제5항 [제5검증: 자동화 테스트 100% 통과]**: `npm test` 263개+ 스모크 테스트 0 실패 유지.
6. **제6항 [게이트키퍼 스크립트 강제 배선]**: `scripts/verify-integrity-gate.js` 물리적 집행 및 AI 픽스처 손수정 금지.

---

## 제8조 (직관적 작업 상태 6단계 보고 헌법 - Status Reporting)
1. **제1항 [직관적 6단계 상태 정의]**:
   - **[1단계: 기획·설계 상태]**: 작업계획서와 규격(REQ/PLAN) 문서만 작성된 상태.
   - **[2단계: 내부 시뮬레이션 상태]**: 작업 브랜치에서 코드 작성 및 테스트 통과 상태 (앱 실제 구동 전).
   - **[3단계: 로컬 수동 확인 상태]**: 개발자의 로컬 환경(`localhost:8000`)에서 화면을 직접 띄워 눈으로 보고 누른 상태.
   - **[4단계: 로컬 메인 병합 상태]**: 작업 브랜치가 로컬 `main`에 합쳐져 로컬 앱에서 실제로 동작하는 상태.
   - **[5단계: 실서버 배포 상태]**: GitHub push되어 Vercel 실서버 배포가 완료되어 실제 웹사이트에서 서비스되는 상태.
   - **[6단계: 실운영 최종 확인 상태]**: 실제 운영 주소(`ourgoal-app.vercel.app`)에 직접 접속해 라이브 환경에서 실동작까지 최종 확인한 상태.
2. **제2항 [보고 시 물리적 의미 명시 의무]**: 모호한 개발자 언어를 전면 금지하고 단계 명칭과 물리적 구동 의미를 명시.

---

## 제9조 (배포 안전핀 및 프리뷰 자동 배포 헌법 - Preview & Deployment Gate)
1. **제1항 [4단계 마감 및 Vercel 프리뷰(5A) 자동 실행]**: 4단계 완료 즉시 질문 없이 Vercel 프리뷰 배포 자동 실행 및 실기기 테스트용 URL 제공.
2. **제2항 [원격 main PR 머지 = 실서버 프로덕션 배포 동일시]**: 상민님의 명시적 승인("배포", "1") 없이 `gh pr merge` 및 `git push origin main`을 실행하는 것은 승인선 4호를 위반한 불법 배포로 규정하여 즉시 무효화.
3. **제3항 [작업계획서 4단계 마감 상한선 엄수]**: 작업계획서 체크리스트는 반드시 [4단계 및 5A 프리뷰 배포]까지만 등록.

---

## 제10조 (용어 헌법: '히트맵' 강제 및 글로벌 I/O 헌법 - Terminology)
1. **제1항 ['잔디' 단어 절대 사용 금지]**: 코드, 주석, UI, 문서 전 영역에서 해당 금지 단어 사용 전면 영구 금지.
2. **제2항 ['히트맵(Heatmap)' 단일 표기]**: 모든 연속성 시각화는 예외 없이 '히트맵'으로만 명명하고 표기.
3. **제3항 [글로벌 I/O 자동 치환]**: 네트워크 진입점에서 글로벌 자동 치환기가 '히트맵'으로 자동 매핑.

---

## 제11조 (Tri-Sync 3자 상호 동기화 및 미디어 통제 헌법 - Tri-Sync & Media Control)
1. **제1항 [Tri-Sync 3자 상호 동기화 원칙]**: Notion(중앙 DB) - Obsidian(로컬 지식) - Command Center(HUD/저널) 3자 실시간 상호 동기화 및 3-Way 자동 중재. 세션 종료 전 `tri-sync.js check` 무결성(100%) 검증.
2. **제2항 [미디어 생성 통제]**: 오직 아워골 아바타 이미지 제작만 허용하며 영상/쇼츠/릴스는 전면 차단.

---

## 제12조 (지시 의도 스코프 및 단계별 멈춤 헌법 - Intent Scope & Boundary Control)
1. **제1항 [5대 지시 모드 분리]**:
   - **모드 1 [탐색·질문 모드]**: 타당성 분석과 답변만 보고하고 대기 (코드 수정 전면 금지).
   - **모드 2 [구상·설계 한정 모드]**: 텍스트 기획/설계안만 제시하고 대기 (저장소 파일 수정 금지).
   - **모드 3 [단계별 명시 정지 모드]**: 해당 단계 완료물 보고 후 대기 (다음 단계 진입 금지).
   - **모드 4-A [완곡한 요청 및 로컬 완결 4단계 모드]**: 5대 무결성 검증 통과 및 [4단계: 로컬 메인 병합 및 5A 프리뷰 배포]까지만 무중단 자율 완결 후 `[결심 필요]` 대기.
   - **모드 4-B [프로덕션 배포 모드]**: 상민님의 명시적 배포 지시("배포", "승인", "1") 시에만 5단계(실서버 배포) 및 6단계(실운영 확인) 진입.
2. **제2항 [승인 없는 5단계 배포 위헌 차단]**: 상민님 승인 없이 5단계 프로덕션 배포 진입 엄격 금지.

---

## 제13조 (실 사용자 계정 상호 연동 헌법 - Real Inter-Account Interaction)
1. **제1항 [실제 구현의 절대 정의]**: 2인 이상 상호작용 기능(DM, 동반자, 팀 톡, 찌르기 등)의 실제 구현은 반드시 **"실제 등록된 사용자들의, 상대방 계정(들)과 서버(DB/Realtime/API) 물리 연동"**만을 인정한다.
2. **제2항 [가짜 실제구현 영구 금지]**: 가상 페르소나 풀, setTimeout 가상 봇 자가답장, 로컬스토리지 자가 순환 루프의 실제 구현 위장 영구 금지.
3. **제3항 [투명한 시스템 안내 및 게스트 모드 보호]**: 콜드스타트 완충용 AI 봇은 `[AI 봇]` 투명 뱃지 표기, 게스트 접속 시 우아한 소프트 로그인 가이드 제공.
4. **제4항 [E3 동류소통 3대 필수 백본]**: ①영속성 원장(DB Engine) + ②실시간 양방향 전파(Realtime Engine) + ③실사용자 식별(Identity Engine) 및 2계정 이상 상호 송수신 통합 테스트 통과 의무화.

---

## 제14조 (외부 연동 E2E 무결성 및 헌법 독점주의 헌법 - Constitutional Exclusivity)
1. **제1항 [외부 앱 의존 껍데기 링크 전면 금지 및 인앱 완결]**: 푸터, 서브메뉴, 모달 내 `mailto:` 등 외부 의존 링크 영구 금지, 모든 고객 접수 창구는 인앱 모달로 단일화.
2. **제2항 [클라우드 환경변수 3자 사전 동기화 (No Silent Failure)]**: 로컬 `.env` 필수 토큰/키는 배포 전 프로덕션 클라우드(Vercel, Supabase)에 사전 동기화(`vercel env ls`).
3. **제3항 [PWA·브라우저 캐시 강제 무효화]**: 프론트엔드 핵심 코드 변경 시 반드시 `sw.js`의 `CACHE_NAME`을 당일/티켓 버전으로 즉시 갱신.
4. **제4항 [6단계 실운영 최종 확인의 2중 물리적 증거 확보]**: 프로덕션 라이브 엔드포인트 실제 요청 전송 후 최종 수신처(노션 DB 행 생성, 텔레그램 알림 수신) 도달 2중 물리적 증거 명시 의무화.
5. **제5항 [법체계 위계 단일화 (조·항·호·목 원칙)]**: 모든 규범과 규칙은 **[조(Article) ➔ 항(Paragraph) ➔ 호(Item) ➔ 목(Sub-item)]** 4단계 표준으로만 표기. 헌법의 대원칙이 '조'이며, 세부 법조문은 모두 '항'이다. 조 번호 난립(21조 등) 영구 금지.
6. **제6항 [헌법 독점주의 (사설 규칙 제정 전면 금지)]**: 유효한 모든 개발·운영·협업 규범은 **오직 본 최고 헌법 정본(`OURGOAL_ABSOLUTE_INTEGRITY_RULES.md`) 안에 직접 편입·등재된 14개 조문과 세부 항만을 인정**한다. 헌법 밖 사설 규칙은 전면 무효다.
7. **제7항 [신규 규칙 제정의 헌법 편입 의무]**: 향후 새로운 규칙·가이드라인은 별도 파일로 분산하지 않고 **반드시 본 헌법에 조/항으로 직접 편입하여 상민님 명시적 승인을 거쳐서만 제정**한다.
8. **제8항 [규범 변경의 절대 승인선 및 기계적 게이트 강제]**: 조/항 개정은 상민님 승인 필수, `scripts/verify-integrity-gate.js` 기계적 단언문으로 영구 배선.

---

## 부칙: 기계적 집행 장치
1. `scripts/verify-integrity-gate.js`: 껍데기 버튼 정적 검사, 10종 페르소나 데이터 무손실 딥이퀄 검증, 14대 조문 및 항 위계 기계적 검증.
2. `scripts/essence-gate.js`: 커밋 태그, 금지 패턴, 승인선 8대, 줄 수 한도 검사.
3. `npm test`: 263개 이상 스모크 테스트 전수 통과.
4. `gate.js` (`C:\dev\command-center\gate\gate.js`): 상민님 공통 지침 및 게이트 훅.
