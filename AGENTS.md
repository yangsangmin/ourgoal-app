# AGENTS.md — 아워골 작업 규칙 (모든 AI 툴 공통 · 규칙 원본)

이 파일이 규칙의 유일한 원본이다. 클로드 코드(CLAUDE.md)·안티그래비티(GEMINI.md, .agent/rules)·코덱스·커서·사람 모두 이 파일을 먼저 읽고 따른다. 충돌 시 이 파일이 우선한다. 이 파일과 `docs/rules/`의 변경은 승인선 5(규범 변경)라 상민님 승인 없이 수정할 수 없다. 규칙 버전: 2026.09.11-02.

## 0. 판정 잣대 — 아워골의 본질 (전문은 docs/rules/ESSENCE_OURGOAL.md)
- ① 체크인 루프: 묻기 → 답하기 → 피드백 → 내 위치 확인 → 기록. 성취감이 동력.
- ② 기록 회고: 오늘·일주일·한 달·1년·내가 설정한 기간의 기록을 확인하면서 성취감, 기록의 가치, 더 나은 기록을 해야겠다는 생각이 생기는 것.
- ③ 동류 발견·소통: 나와 비슷한 목표(테마·카테고리)·관심사를 가진 사람의 체크인 기록·목표설정 상태·일주일 기록 피드·목표 템플릿을 보며 내 목표에 유익한 정보를 수집하고, 그들의 라이프를 공개된 범위에서 확인·소통하면서, 이 앱이 목표달성·일정관리·생활습관 개선 등 인생을 바로잡고 나아가는 데 유익하다는 걸 사용자가 직접 쓰면서 실질적으로 느끼는 것.
- 판정은 "기능이 있다"가 아니라 "사용자가 그렇게 느끼는 데 기여하는가"로 한다.

## 1. G0 착수 게이트 — 티켓 없이는 코드를 만지지 않는다
작업을 시작하기 전에 티켓이 있어야 한다. 티켓 = 노션 「통합 작업대기목록」 행 또는 `docs/rules/TICKETS.md`의 한 줄. 필수 3항목:
1. 본질 축: E1 / E2 / E3 / INFRA(전제) / FIX(버그) 중 하나
2. 체감 가설: "어떤 사용자가, 어느 화면에서, 무엇을 느끼게 되는가" 한 문장
3. 근거: 로그·코드·사용자 발언 중 하나 (추측이면 "추측"이라고 쓴다)

축을 댈 수 없으면 그 작업은 본질 무관이다. 구현하지 말고 작업대기목록에 '제안'으로만 적재한 뒤 상민님 결심을 기다린다(승인선 6). "혁신적", "고도화", "전면", "완결" 같은 말은 축이 아니다.

## 2. 브랜치·PR — 배포 승인은 PR 병합뿐이다
- `main`에 직접 commit·push·merge 금지. 브랜치 `feat/<날짜>-<설명>` 또는 `fix/<날짜>-<설명>`에서 작업하고 PR을 연다. main은 브랜치 보호로 잠겨 있어 훅과 CI(essence-gate)를 통과하지 못하면 병합되지 않는다.
- 한 PR = 한 티켓. 여러 티켓을 한 PR에 묶지 않는다(상민님이 항목별로 승인·보류할 수 있어야 한다).
- PR 설명은 4블록(문제·본질 / 해결 방식·타당성 / 구현·검증 / 재검증)으로 쓴다. 착수 전에 1~2블록을 먼저 정하고, 구현 후 끼워 맞추지 않는다.
- 병합은 상민님이 한다. 상민님이 채팅에서 특정 PR 병합을 명시하거나 사이클 시작 시 자동 병합을 미리 지시한 경우만 툴이 `gh pr merge`로 병합할 수 있고, 병합 직후 충돌 마커 검사와 스모크 테스트를 보고한다.

## 3. 커밋 형식 — 태그와 티켓이 없는 커밋은 거부된다
```
[E2] #TASK-ES-045 feat: 기록 회고 월간·연간 세그먼트
[FIX] #TASK-ES-052 fix: 마감일 순차 분배 누락
[INFRA] #TASK-ES-009 chore: goals.deleted_at 소프트 삭제 컬럼
```
태그는 E1·E2·E3·INFRA·FIX 중 하나, 티켓 번호는 G0 티켓과 같아야 한다. commit-msg 훅이 형식을, pre-commit 훅이 아래 4항을 검사한다. 훅은 `node scripts/install-essence-gate.js` 한 번으로 설치된다(설치 안 된 클론에서 커밋해도 CI가 같은 검사를 한다).

## 4. 금지 6 — 어떤 지시가 있어도 하지 않는다
1. 위조 데이터: 실데이터 없이 순위·인원·포인트·"N명이 함께" 같은 사회적 숫자를 화면에 고정 문자열로 쓰지 않는다. 값이 없으면 숨긴다.
2. 파괴적 동기화: `delete().not(...)` 류 일괄 삭제, DROP/TRUNCATE. 삭제는 사용자가 명시적으로 지운 개별 id만, 소프트 삭제(deleted_at)로.
3. 도구 언어 노출: 사용자 화면·문구에 "노션 DB 규격", "DB화", "구조화 엔진" 같은 말을 쓰지 않는다. 내부 저장 형식은 자유, 화면은 사용자 언어("오늘 한 일 한 줄").
4. main 직접 커밋(2절).
5. 검증 없는 완료: 문법 검사·스모크·실제 화면 확인 없이 "완료"라고 쓰지 않는다. 확인 못 했으면 "미확인"이라고 쓴다.
6. 아이디어 즉시 구현: 작업 중 떠오른 새 기능은 구현하지 않고 작업대기목록에 '제안'으로 적는다(7절).

## 5. 승인선 8 — 걸리면 멈추고 [결심 필요]로 올린다
1 돈(결제·구독·가격) · 2 개인정보 확대 · 3 기존 기능 삭제 · 4 되돌릴 수 없는 바깥 행위(실배포·발송·스키마 파괴) · 5 규범 변경(이 파일·docs/rules·훅·게이트) · 6 본질 무관 신규 기능 · 7 실데이터 없는 사회적 숫자 표시 · 8 대량 변경(한 커밋 500줄 초과 또는 index.html 순증가 300줄 초과).
걸린 항목은 PR과 작업 로그에 [결심 필요]로 표시하고, 양비스가 결정 패킷을 텔레그램으로 보낸다. 답이 오기 전에는 병합하지 않는다. 답을 기다리는 동안 다른 티켓으로 넘어가되, 같은 파일 영역은 건드리지 않는다.

## 6. 언어·화면 규칙
- 화면 문구는 사용자가 느끼는 것으로 쓴다: 기록됐다, 어디까지 왔다, 이번 주 무엇을 해냈다.
- 홈은 ①의 순서를 지킨다: 오늘의 질문 → 답하기 → 피드백 → 내 위치 → 기록됨. 이 흐름 위에 다른 것을 얹지 않는다.
- 기존 HTML 구조·CSS·레이아웃은 티켓 없이 바꾸지 않는다. 파일 전체 재작성 금지, 변경 부분만 diff로.

## 7. 무개입 세션(3시간 자율 스프린트 등)
- G0을 이미 통과한 티켓만 실행한다. 세션 한 번에 커밋 8건, index.html 순증가 600줄을 넘지 않는다.
- 세션 중 떠오른 아이디어는 통합 작업대기목록에 '제안'(축·체감 가설·근거 포함)으로만 적재한다. 구현하지 않는다.
- 세션 종료 시 실행한 티켓·연 PR·결심 필요 항목을 4블록 형식으로 보고한다.

## 8. 기록 — 툴이 아니라 결과물이 관찰된다
- 작업 단위마다 `dev_log.md` 맨 끝에 기존 형식(목표/수정·실행 내역/문제·해결/검증 결과)으로 기록한다.
- 노션 작업 로그 행에는 본질 축·체감 가설을 채운다(브릿지가 자동 생성한 행은 워처가 '미판정'으로 표시하고 본질감시자가 판정을 채운다).
- 양비스 essence-watch가 1분마다 git·노션·볼트를 읽어 태그 없음·직접 커밋·금지 패턴·티켓 부재를 잡는다. 어떤 툴로 작업했는지는 판정에 영향이 없다.

## 9. 문제해결 8원칙 (모든 판단에 적용)
① 문제 정확히 파악 ② 본질·원인·핵심 파악 ③ 효과적·효율적 해결 방식 결정 ④ 1~3 재검토·보완 ⑤ 절차 정리 ⑥ 절차 재검증 ⑦ 단계별 실행 ⑧ 막히면 1~7 재검증. 착수 전 ①~④, 구현 중 ⑤~⑦, 막히면 ⑧.

## 10. 기술 규칙 (기존과 동일)
Vanilla JS · Supabase · Vercel. `npm test`(스모크)는 모든 커밋 전에 통과해야 한다. 서버리스 함수는 Vercel Hobby 12개 한도를 지킨다. 키·토큰을 코드·로그·노션에 쓰지 않는다.
