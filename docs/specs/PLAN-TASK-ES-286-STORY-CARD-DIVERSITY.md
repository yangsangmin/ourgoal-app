# [PLAN] #TASK-ES-286: 갓생 스토리카드 다각화(1:1·3:4·9:16 비율 및 항목 선택) 및 피드 즉시 게시 기능 구현

## 1. 계획 개요 (원칙 ① 상민님 직접 지시)
- **티켓**: `#TASK-ES-286` (노션 생각 메모장 `[35]`번)
- **본질 축**: `E2 / RPG / UX`
- **상민님 지시 원문**:
  > *"갓생 스토리카드 1대1비율 3대4비율(가로세로모두) 9대16비율(가로세로모두) 선택 가능하게 바꾸고, 넣을 수 있는 항목 선택 가능하게 해. 각 목표, 내 아바타, 기록, ai 피드백 선택해서 각 경우의 수별로 모두 이쁘게 담기게 세팅해. 그리고 ‘아워골 피드게시’도 만들고 누르면 바로 피드에 게시되게 해."*

---

## 2. 구현 전략 및 아키텍처 (원칙 ② 본질·원인·중심·핵심)
- **원인 분석**: 스토리카드 비율이 고정되어 인스타 스토리/피드 등 목적별 규격을 만족하지 못하고 피드 연계가 끊겨있던 것이 공유 저조의 **원인**임.
- **본질 가치**: 유저의 노력과 성취를 아름답게 담아내어 즉각 동류들과 나누는 루프를 완성하는 것이 **본질**임.
- **중심 해결책**: 1:1, 3:4, 9:16 다각화 비율 및 항목 선택 커스텀 엔진과 즉시 피드 게시 파이프라인에 개발의 **중심**을 둔다.
- **핵심 목표**: 스토리카드 생성 모달과 원클릭 피드 게시 연동을 고도화하여 사용자 **핵심** 체감 만족도를 극대화한다.

---

## 3. 단계별 상세 계획

### 3.1 4위 1체 마크업 및 스타일링 (원칙 ⑤, ⑥, ⑦)
- `index.html`: `#og-task-35-container`, `#og-task-35-action-btn` 마운트.
- `ui.css`: `#og-task-35-container`, `#og-task-35-action-btn` 전용 클래스 정의.
- 모바일 375px 대응 및 터치 타겟 44px 이상, 0px 오버플로우 보장.

### 3.2 직통 비즈니스 로직 연동 (원칙 ⑤, ⑥)
- `js/components.js` & `js/records-stats.js`: `handle기록스톱워치_Item35Action(event)` 구현 및 전역 export.
- 12ms 햅틱 피드백, 200ms 디바운스, `og_task-35_cache` 로컬 캐시 원자적 갱신.
- 4대 뷰 원자적 동시 전파 (`renderCalendar`, `renderGoalsScreen`, `renderHome`, `renderRecordsScreen`).

### 3.3 회귀 방지 및 검증 (원칙 ③, ④, ⑧)
- `tests/story-card-diversity.test.js` 작성.
- `scripts/smoke-test.js`에 `#TASK-ES-286` 단언문 추가.
- `reports/TASK-ES-286/claims.json` 작성 및 헌법 38대 게이트 통과.
- GitHub PR 생성 및 Court 심사 청구.
