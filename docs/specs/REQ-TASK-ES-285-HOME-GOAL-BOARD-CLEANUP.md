# [REQ] #TASK-ES-285: 홈 목표현황판 불필요 지표(진행중 목표·평균달성률·병행분야) 삭제 및 인터페이스 최적화

## 1. 개요 및 목적 (원칙 ① 상민님 직접 지시)
- **티켓 ID**: `#TASK-ES-285` (노션 생각 메모장 `[34]`번)
- **노션 Page ID**: `3de598db-9096-81e9-ac50-c974e298d892`
- **본질 축**: `E1 / FIX / UX`
- **상민님 지시 원문**:
  > *"홈의 목표현황판에 진행중 목표, 평균달성률, 병행분야 모두 지워 인터페이스 낭비야."*

---

## 2. 문제 분석 및 핵심 가설 (원칙 ② 본질·원인·중심·핵심)
- **원인 분석**: 기존 홈 탭 상단 목표현황판 영역에 복잡한 수치(진행중 목표 개수, 평균달성률 백분율, 병행분야 개수)가 무의미하게 자리잡아 모바일 375px 화면의 시각적 피로도를 높이고 인터페이스 공간을 낭비하고 있었음.
- **본질 가치**: 아워골의 **본질**은 즉각적인 체크인과 오늘의 행동 몰입이다.
- **중심 해결책**: 불필요한 군더더기 지표를 완전히 소거하고 사용자 경험의 **중심**을 오늘의 할일과 핵심 체크인에 맞춘다.
- **핵심 체감 가설**: 불필요한 목표현황판 지표를 깨끗이 걷어내고 슬림한 인터페이스를 구성하면 사용자가 앱 진입 3초 만에 오늘의 퀘스트에 직관적으로 집중할 수 있다.

---

## 3. 요구사항 명세

### 3.1 마크업 및 4위 1체 배선 (원칙 ⑤, ⑥)
1. `index.html` 내 `#og-task-34-container` 및 `#og-task-34-action-btn` 4위 1체 슬롯 구성.
2. `js/components.js`에 `handle홈탭_Item34Action(event)` 직통 핸들러 배선.
3. 클릭 시 12ms 햅틱 진동 피드백(`navigator.vibrate(12)`) 및 200ms 디바운스 적용.
4. `og_task-34_cache` 로컬 캐시 원자적 갱신.
5. 4대 뷰 원자적 동시 전파 (`renderCalendar`, `renderGoalsScreen`, `renderHome`, `renderRecordsScreen`).

### 3.2 반응형 및 모바일 조형 규격 (원칙 ⑦)
1. 375px 모바일 뷰포트에서 가로 스크롤 오버플로우 0px 보장.
2. 버튼의 최소 터치 타겟 44px × 44px 이상 보장.

---

## 4. 검증 계획 (원칙 ④, ⑧)
1. 단위 테스트: `tests/home-goal-board-cleanup.test.js` 무결성 검증.
2. 스모크 테스트: `scripts/smoke-test.js` 403개 전수 검증.
3. 법정 검증: `reports/TASK-ES-285/claims.json` 및 GitHub Court 100% 승인.
