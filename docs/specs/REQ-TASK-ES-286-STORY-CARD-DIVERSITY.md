# [REQ] #TASK-ES-286: 갓생 스토리카드 다각화(1:1·3:4·9:16 비율 및 항목 선택) 및 피드 즉시 게시 기능 구현

## 1. 개요 및 목적 (원칙 ① 상민님 직접 지시)
- **티켓 ID**: `#TASK-ES-286` (노션 생각 메모장 `[35]`번)
- **노션 Page ID**: `3de598db-9096-817e-9014-f75ef65aa4b9`
- **본질 축**: `E2 / RPG / UX`
- **상민님 지시 원문**:
  > *"갓생 스토리카드 1대1비율 3대4비율(가로세로모두) 9대16비율(가로세로모두) 선택 가능하게 바꾸고, 넣을 수 있는 항목 선택 가능하게 해. 각 목표, 내 아바타, 기록, ai 피드백 선택해서 각 경우의 수별로 모두 이쁘게 담기게 세팅해. 그리고 ‘아워골 피드게시’도 만들고 누르면 바로 피드에 게시되게 해."*

---

## 2. 문제 분석 및 핵심 가설 (원칙 ② 본질·원인·중심·핵심)
- **원인 분석**: 기존 스토리카드가 단일 규격 비율과 정적인 항목만 노출하여 다양한 소셜 플랫폼(인스타 스토리, 피드 등) 규격에 부응하지 못하고 즉각적인 피드 공유 파이프라인이 부재했던 것이 공유율 저하의 **원인**임.
- **본질 가치**: 유저가 자신의 성취와 성장을 멋지게 시각화하여 동류에게 자랑하고 자긍심을 느끼는 것이 **본질**임.
- **중심 해결책**: 1:1, 3:4, 9:16 다각화 비율 및 목표·아바타·기록·AI피드백 자유 조합 엔진과 원클릭 피드 발행을 인터페이스의 **중심**에 둔다.
- **핵심 체감 가설**: 취향에 맞춘 비율과 데이터 항목을 선택하고 원클릭으로 피드에 즉시 게시할 수 있게 하면 **핵심** 성취감과 소통 참여율이 2배 이상 증대된다.

---

## 3. 요구사항 명세

### 3.1 마크업 및 4위 1체 배선 (원칙 ⑤, ⑥)
1. `index.html` 내 `#og-task-35-container` 및 `#og-task-35-action-btn` 4위 1체 슬롯 구성.
2. `js/components.js` 및 `js/records-stats.js`에 `handle기록스톱워치_Item35Action(event)` 직통 핸들러 배선.
3. 클릭 시 12ms 햅틱 진동 피드백(`navigator.vibrate(12)`) 및 200ms 디바운스 적용.
4. `og_task-35_cache` 로컬 캐시 원자적 갱신.
5. 4대 뷰 원자적 동시 전파 (`renderCalendar`, `renderGoalsScreen`, `renderHome`, `renderRecordsScreen`).

### 3.2 반응형 및 모바일 조형 규격 (원칙 ⑦)
1. 375px 모바일 뷰포트에서 가로 스크롤 오버플로우 0px 보장.
2. 버튼의 최소 터치 타겟 44px × 44px 이상 보장.

---

## 4. 검증 계획 (원칙 ④, ⑧)
1. 단위 테스트: `tests/story-card-diversity.test.js` 무결성 검증.
2. 스모크 테스트: `scripts/smoke-test.js` 404개 전수 검증.
3. 법정 검증: `reports/TASK-ES-286/claims.json` 및 GitHub Court 100% 승인.
