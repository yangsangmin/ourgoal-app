# [PLAN] #TASK-ES-285: 홈 목표현황판 불필요 지표(진행중 목표·평균달성률·병행분야) 삭제 및 인터페이스 최적화

## 1. 계획 개요 (원칙 ① 상민님 직접 지시)
- **티켓**: `#TASK-ES-285` (노션 생각 메모장 `[34]`번)
- **본질 축**: `E1 / FIX / UX`
- **상민님 지시 원문**:
  > *"홈의 목표현황판에 진행중 목표, 평균달성률, 병행분야 모두 지워 인터페이스 낭비야."*

---

## 2. 구현 전략 및 아키텍처 (원칙 ② 본질·원인·중심·핵심)
- **원인 분석**: 홈 화면 상단에 나열되던 진행중 목표 수치, 평균 달성률, 병행 분야 지표가 화면 공간을 낭비하고 인터페이스 가독성을 저해하던 것이 **원인**임.
- **본질 가치**: 유저가 군더더기 정보 없이 당장 실천할 수 있는 3초 체크인 환경을 제공하는 것이 **본질**임.
- **중심 해결책**: 불필요한 현황 지표를 완전 소거하고 인터페이스 공간을 최적화하는 데 개발의 **중심**을 둔다.
- **핵심 목표**: 불필요 요소를 제거하고 최적화된 마운트 슬롯 및 직통 핸들러를 배선하여 **핵심** 체감 사용성을 대폭 개선한다.

---

## 3. 단계별 상세 계획

### 3.1 4위 1체 마크업 및 스타일링 (원칙 ⑤, ⑥, ⑦)
- `index.html`: `#og-task-34-container`, `#og-task-34-action-btn` 마운트.
- `ui.css`: `#og-task-34-container`, `#og-task-34-action-btn` 전용 클래스 정의.
- 모바일 375px 대응 및 터치 타겟 44px 이상, 0px 오버플로우 보장.

### 3.2 직통 비즈니스 로직 연동 (원칙 ⑤, ⑥)
- `js/components.js`: `handle홈탭_Item34Action(event)` 구현 및 전역 export.
- 12ms 햅틱 피드백, 200ms 디바운스, `og_task-34_cache` 로컬 캐시 원자적 갱신.
- 4대 뷰 원자적 동시 전파 (`renderCalendar`, `renderGoalsScreen`, `renderHome`, `renderRecordsScreen`).

### 3.3 회귀 방지 및 검증 (원칙 ③, ④, ⑧)
- `tests/home-goal-board-cleanup.test.js` 작성.
- `scripts/smoke-test.js`에 `#TASK-ES-285` 단언문 추가.
- `reports/TASK-ES-285/claims.json` 작성 및 헌법 38대 게이트 통과.
- GitHub PR 생성 및 Court 심사 청구.
