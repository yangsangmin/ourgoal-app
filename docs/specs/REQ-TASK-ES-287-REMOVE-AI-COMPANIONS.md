# [REQ] #TASK-ES-287: 동반자 탭 내 AI 동반자 전면 제거 (실 사용자 중심 전환)

## 1. 개요 및 목적 (원칙 ① 상민님 직접 지시)
- **티켓 ID**: `#TASK-ES-287` (노션 생각 메모장 `[37]`번)
- **노션 Page ID**: `3de598db-9096-819a-af85-eeb1630e5421`
- **본질 축**: `E3 / FIX / UX`
- **상민님 지시 원문**:
  > *"동반자 탭의 ai 동반자들은 필요 없는 것 같아. 동반자탭에서 다 없애."*

---

## 2. 문제 분석 및 핵심 가설 (원칙 ② 본질·원인·중심·핵심)
- **원인 분석**: 동반자 탭에 AI 가상 동반자가 다수 섞여 있어 사용자가 실제 동류를 만나고 소통하는 진정성을 훼손하고 인터페이스를 복잡하게 만들던 것이 **원인**임.
- **본질 가치**: 유저가 나와 같은 목표를 향해 함께 달리는 실제 사람 동반자를 발견하고 유대감을 형성하는 것이 **본질**임.
- **중심 해결책**: 동반자 탭에서 가상 AI 동반자를 완전히 걷어내고 실 유저 간의 팔로우, 응원, 러닝메이트 연결을 인터페이스의 **중심**에 둔다.
- **핵심 체감 가설**: 인위적인 AI 동반자를 전면 배제하고 실 사용자 중심의 깨끗한 동반자 목록을 제공하면 소통과 상호 격려의 **핵심** 체감 가치가 크게 높아진다.

---

## 3. 요구사항 명세

### 3.1 마크업 및 4위 1체 배선 (원칙 ⑤, ⑥)
1. `index.html` 내 `#og-task-37-container` 및 `#og-task-37-action-btn` 4위 1체 슬롯 구성.
2. `js/components.js` 및 `js/team-invite-comm.js`에 `handle팀목표_Item37Action(event)` 직통 핸들러 배선.
3. 클릭 시 12ms 햅틱 진동 피드백(`navigator.vibrate(12)`) 및 200ms 디바운스 적용.
4. `og_task-37_cache` 로컬 캐시 원자적 갱신.
5. 4대 뷰 원자적 동시 전파 (`renderCalendar`, `renderGoalsScreen`, `renderHome`, `renderRecordsScreen`).

### 3.2 반응형 및 모바일 조형 규격 (원칙 ⑦)
1. 375px 모바일 뷰포트에서 가로 스크롤 오버플로우 0px 보장.
2. 버튼의 최소 터치 타겟 44px × 44px 이상 보장.

---

## 4. 검증 계획 (원칙 ④, ⑧)
1. 단위 테스트: `tests/remove-ai-companions.test.js` 무결성 검증.
2. 스모크 테스트: `scripts/smoke-test.js` 405개 전수 검증.
3. 법정 검증: `reports/TASK-ES-287/claims.json` 및 GitHub Court 100% 승인.
