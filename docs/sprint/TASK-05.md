# [TASK-05] 맥락 기반 AI 다이내믹 푸시 알림 고도화

| 항목 | 내용 |
|---|---|
| 우선순위 | P1 (리텐션) |
| 카테고리 | 푸시/알림 |
| 기대효과 | D7/D30 재방문율 3배 개선, Mute 차단 방지 |
| 대상파일 | index.html, sw.js, Web Push API |
| 의존 태스크 | 없음 |
| 노션 진행상태(내보내기 시점) | 시작 전 |

## 기존 코드/PR과 겹치는 부분 (착수 전 확인)
- origin/main에 PR #34 "Service Worker 기반 Web Push 알림"이 이미 병합됨. 이 태스크는 그 구현 위에 `generateDynamicNotification`을 얹는 것이다. 착수 전 #34의 setupNotifyTimer·sw.js 핸들러 구조를 먼저 읽는다.
- sw.js도 수정 대상이므로 스모크 테스트 외에 `node --check sw.js`와 서비스워커 재등록 확인이 필요하다.
- 스트릭은 기존 computeStreakDays, D-day는 기존 dDay 함수를 재사용한다(둘 다 스모크 테스트 대상 함수).

## 사용자 필요 작업 (외부 콘솔·키, 코드에 넣지 않음)
- 없음 (#34에서 푸시 설정이 끝났다면). VAPID 키 등이 미설정이면 #34 PR 본문의 안내를 따른다.

## 실행 프롬프트 (노션 원문, 2026-09-06 내보내기)

> 프롬프트의 코드 예시는 참고용이다. 기존 코드의 함수명·상태 구조·디자인 토큰이 우선한다.

현재 고정 문구('지금 뭐 하고 있었어요?')로 발송되는 웹 푸시 알림을 유저의 실시간 목표 데이터와 D-day, 스트릭 상태에 맞춰 지능적으로 생성되는 '맥락 기반 다이내믹 알림'으로 고도화해주세요.

1. 요구사항:
- 푸시 메시지 생성 팩토리 함수 generateDynamicNotification(profile)를 구현하세요:
  1. 마감일 임박 목표가 있는 경우 (D-3 이내):
     '[D-day 임박] {목표명} 마감까지 {N}일 남았어요! 오늘 마일스톤을 체크해보세요 🔥'
  2. 연속 스트릭 위기 (당일 체크인 기록이 없고 저녁 8시 이후인 경우):
     '[스트릭 경보] {N}일 연속 불꽃이 꺼지기 직전이에요! 오늘 한 일을 10초 만에 남겨보세요 ⏰'
  3. 참여 중인 모임/마니또가 있는 경우:
     '[모임 인증] {모임명} 팀원들이 오늘 {N}회 인증했어요! 함께 달려볼까요? 🏃'
  4. 기본 메시지:
     '{displayName}님, 오늘의 성장을 기록할 시간이에요 ✨'
- setupNotifyTimer 및 Service Worker postMessage 전송 로직에서 위 다이내믹 메시지를 타이틀/바디로 전달하도록 연결하세요.

2. 수정 대상: index.html 내 setupNotifyTimer, showNotifyBanner, sw.js 알림 핸들러
3. 검증 기준: '지금 테스트해보기' 버튼 클릭 시 유저의 실제 목표 상태(마감일/스트릭)를 반영한 동적 텍스트가 알림에 노출되는지 확인.
