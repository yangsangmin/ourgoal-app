# 법정 판정서

> **이 출력은 작업자 PC 에서 돌린 예비 점검입니다. 판정 효력이 없습니다.** 판정은 GitHub 의 법정(작업자가 고칠 수 없는 곳)에서 나온 것만 인정됩니다.

**판정: 돌려보냄(다시 해야 함) — 되던 기능 4개가 고장 났습니다 (그 밖의 사유 1건)**  
**상민님이 하실 일: 없음. 작업자가 고쳐서 다시 심사받아야 합니다. 지금은 “1”을 눌러 배포할 상태가 아닙니다.**  
**지시 항목 기준 분포: 없음(작업자가 주장을 내지 않았습니다)**  
**심사 대상 커밋 1c61f5f (기준 15df641) · 판정번호 049D5156 · 작업자 PC 에서 실행한 예비 점검**  

## 돌려보낸 이유
1. **주장 없음** — 제품 코드 5개 파일을 바꿨는데 이번 변경에 reports/‹작업번호›/claims.json 이 없다. 무엇을 했는지 주장하지 않으면 법정은 회귀만 볼 수 있을 뿐 "됐다"를 확인해 줄 수 없다.
2. **되던 부품이 고장 남** — js/reactions.js — ReferenceError: init is not defined → 이 모듈이 등록하던 전역(OurgoalReactions)이 앱에 없게 된다
3. **되던 기능이 고장 남** — 앱을 띄우기만 해도 새 오류가 난다: ReferenceError: init is not defined ＠ /js/reactions.js (기준 0회 → 작업 1회)
4. **되던 기능이 고장 남** — 표준 점검 "새 목표 창을 어두운 배경을 눌러 닫으면 제자리에 머물고 종료 안내가 뜨지 않는다" 이 작업 커밋에서 실패(단계 9): #toast.show → 관찰 1500ms 중 나타남(보이는 글자: ["한 번 더 누르면 앱이 종료됩니다."], 1번째 확인)
5. **되던 기능이 고장 남** — 표준 점검 "목표 탭에서 새 목표 창을 어두운 배경을 눌러 닫으면 목표 탭에 그대로 머문다(홈으로 튕기지 않는다)" 이 작업 커밋에서 실패(단계 12): .navbtn[data-tab="goals"] → class="navbtn"

## 알아 두실 것
- **자기가 낸 시험** — 작업과 함께 새로 만든 검증 스크립트다. 자기가 낸 시험이므로 그 결과(예: "N/N ALL PASS")는 증거로 채택하지 않는다. [scripts/verify-master-integrity.js]
- **이 작업이 만든 검사 스크립트 1개** — 그 출력은 증거로 인정하지 않는다: scripts/verify-master-integrity.js

## 지시 항목별 결과
주장 파일(claims.json)이 없어 지시 항목별 결과를 낼 수 없습니다.

## 주장과 무관하게 법정이 직접 본 것
- 부품 로드 점검: 작업 커밋 25/27개 로드됨 (기준 커밋 26/27) · 점검 환경 한계로 못 본 부품 1개: js/goal-templates-registry.js
- 기준 시험지 채점(scripts/smoke-test.js): 기준 커밋의 검사 335개 중 작업 커밋에서 335개 통과 — 작업자가 고친 테스트는 판정에 쓰지 않았습니다
- 기준 시험지 채점(scripts/verify-integrity-gate.js): 기준 커밋의 검사 37개 중 작업 커밋에서 37개 통과 — 작업자가 고친 테스트는 판정에 쓰지 않았습니다
- 앱 띄우기: 기준 커밋 뜸 / 작업 커밋 뜸 · 띄울 때 난 오류 0건 → 1건
- 표준 점검 “첫 화면에서 ‘로그인 없이 둘러보기’를 누르면 홈 화면으로 들어간다”: 기준 통과 / 작업 통과
- 표준 점검 “새 목표 창을 열고 뒤로가기를 누르면 창만 닫힌다”: 기준 통과 / 작업 통과
- 표준 점검 “새 목표 창을 어두운 배경을 눌러 닫으면 제자리에 머물고 종료 안내가 뜨지 않는다”: 기준 통과 / 작업 **실패**(toast.show → 관찰 1500ms 중 나타남(보이는 글자: ["한 번 더 누르면 앱이 종료됩니다."], 1번째 확인))
- 표준 점검 “목표 탭에서 새 목표 창을 어두운 배경을 눌러 닫으면 목표 탭에 그대로 머문다(홈으로 튕기지 않는다)”: 기준 통과 / 작업 **실패**(.navbtn[data-tab="goals"] → class="navbtn")
- 표준 점검 “아래 메뉴에서 기록 탭을 누르면 기록 탭으로 바뀌고, 홈을 누르면 홈으로 돌아온다”: 기준 통과 / 작업 통과

## git 사실 (법정이 git 에서 직접 읽음)
- 작업 커밋: `1c61f5f11bdaf00c3618c93e62a31455c5237c7f` / 기준 커밋: `15df64103fc7f32a23920cf640ddd76b0b879537`
- GitHub 에 올라가 있는가: **아니오 — 이 PC 에만 있는 커밋입니다**
- GitHub main 대비: 2커밋 앞섬 / 0커밋 뒤짐
- 바뀐 파일 7개: 제품 코드 5 · 채점 기준(금고) 0 · 테스트 0 · 그 밖 2

---
법정 도구: node v24.15.0 · Chrome/153.0.8010.48 · 소요 161초 · 이 문서는 기계가 만들었습니다.
