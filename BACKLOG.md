# 아워골 백로그

자동화 루틴("분신 직원")이 5시간마다 이 목록에서 **체크되지 않은 항목 중 가장 위에 있는 것 하나**를 골라 작업합니다.
사람이 언제든 이 파일의 순서를 바꾸거나 항목을 추가/삭제할 수 있고, 다음 주기부터 바로 반영됩니다.

작업 방식(중요)은 CLAUDE.md 6번 항목을 따릅니다: 새 브랜치 → PR → 사용자 병합 시에만 실배포.

---

## 진행 중 / 대기

- [ ] PWA 설치 지원: manifest.json + 최소 서비스워커 추가해서 홈 화면에 앱처럼 설치 가능하게. 오프라인 시 최소한 빈 화면 대신 안내 문구 노출.
- [ ] 실제 브라우저 푸시 알림: 지금은 탭이 열려있을 때만 알림이 옴(Notification API). Service Worker 기반 Web Push로 앱이 꺼져 있어도 체크인 시간에 알림 오도록 개선. (메모 2026-09-04: 이 프로젝트엔 npm 패키지가 없어 서버에서 `web-push` 라이브러리 없이 VAPID JWT 서명 + payload AES-GCM 암호화를 Node `crypto`로 직접 구현해야 함. 실제 푸시 서비스(FCM 등)로 왕복 검증할 브라우저 환경이 이 자동화 세션엔 없어, 한 사이클 안에 안전하게 구현·검증하기엔 리스크가 커서 이번엔 보류하고 다음 항목으로 넘어감. 다음에 이 항목을 고를 때는 시간을 더 확보하거나 사람이 Vercel에서 실제 브라우저로 라이브 검증해줄 것을 권장.)
- [ ] 다크모드: 현재 디자인 토큰(CSS 변수) 기준으로 prefers-color-scheme 다크 팔레트 정의하고 자동 전환 지원.
- [ ] 데이터 내보내기 개선: 설정의 내보내기가 현재 요약 위주인데, 체크인 전체 이력을 CSV/JSON으로 온전히 내려받을 수 있게 보강.
- [ ] 접근성 점검: 주요 버튼·입력에 aria-label 보강, 색상 대비(WCAG AA) 확인 및 낮은 대비 텍스트 수정.
- [ ] 온보딩 개선: 첫 목표 생성 후 마니또·모임·공유 등 소통 기능을 자연스럽게 안내하는 짧은 투어/툴팁 추가(기존 온보딩 흐름 유지, 추가 단계만 삽입).
- [ ] 그룹(모임) 랭킹·배지 다듬기: 현재 mock 데이터 기반인 랭킹 UI에 연속 인증일수 기반 배지(예: 🔥3일/🔥7일)를 모임 멤버 카드에도 표시.

## 메타(엔지니어링 기반, 우선순위 높음)

- [ ] 최소 자동 스모크 테스트 스크립트 작성: Node 기반으로 `<script>` 문법 검증 + 핵심 함수(goalProgress, applySuggestion 등) 단위 테스트 몇 개를 `scripts/smoke-test.js` 같은 파일로 만들어서, 다음 자동화 사이클부터는 이걸 먼저 돌려보고 작업하도록. (사람이 매번 라이브 QA 하지 않아도 최소 안전망 확보)

## 완료됨

- [x] 주간/월간 리포트 화면: 기록 탭에 7일/30일 토글 가능한 SVG 추이 차트 + 카테고리별(TOPICS 6분야) 시간 분포 도넛 차트 추가. 기록에 분야(category) 선택 필드 신설 (2026-09-04, PR 대기)
- [x] 프론트엔드 전면 리디자인, 목표 편집 UX 개선, 마니또 탭 (2026-09-04)
- [x] 하이브리드 다중 목표 대시보드, 기록+AI피드백 선택 공유 (2026-09-04)
- [x] 소통 탭 순서 변경, 공유 탭 플랫폼 맞춤 카드(실제 이미지 미리보기) (2026-09-04~05)
- [x] 기록 안내 문구, 기록 기반 목표 자동 업데이트 제안(상태/결과 메모), 구글 캘린더 연동 (2026-09-04~05)
- [x] 맞춤 피드백 봇 설정(대화형 생성 + 검증 + 수동 설정) (2026-09-05)
