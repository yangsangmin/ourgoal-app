# 아워골 백로그

자동화 루틴("아워골 1호 직원")이 6시간마다 이 목록에서 **체크되지 않은 항목**을 위에서부터 골라 한 사이클(최대 1시간)에 여러 개까지 작업합니다.
사람이 언제든 이 파일의 순서를 바꾸거나 항목을 추가/삭제할 수 있고, 다음 주기부터 바로 반영됩니다.

작업 방식(중요)은 CLAUDE.md 6번 항목을 따릅니다: 새 브랜치 → PR → 사용자 병합 시에만 실배포.

---

## 사용자 경험·도파민 강화 (2026-09-05, 사용자 승인 · 우선순위 최상위)

아래 12개는 목표설정·기록·소통을 할 때 성취감·재미·도파민을 강화하기 위해 사용자가 직접 승인한 항목이다. 표시된 단계 순서대로 위에서부터 처리한다. 새 UI 요소를 추가하되 기존 디자인 언어(색상 토큰, 카드 스타일 등)를 그대로 재사용하고, 다크패턴(불안 조장형 FOMO, 과도한 상실회피)은 쓰지 않는다.

- [x] 1단계·즉각 보상: 체크인/할 일 완료 시 축하 마이크로 애니메이션(컨페티·펄스 등, 외부 라이브러리 없이 CSS/캔버스로 구현). (2026-09-05, PR로 제출)
- [x] 1단계·즉각 보상: 마일스톤 완료 시 축하 모달 + AI가 다음 행동을 한 줄로 제안(goalstatus.js 계열의 단일 호출+자체검증 패턴 재사용). (2026-09-05, PR로 제출)
- [x] 1단계·즉각 보상: 기록 히트맵(GitHub 잔디 스타일) — 최근 몇 달간의 꾸준함이 한눈에 보이는 시각화, 기록 탭에 추가. (2026-09-05, PR로 제출)
- [x] 2단계·누적 성장: XP/레벨 시스템 데이터 모델과 계산 로직(체크인·마일스톤 완료 시 XP 적립, settings에 저장). (2026-09-05, PR로 제출 — UI는 다음 항목에서 이어감)
- [x] 2단계·누적 성장: 레벨 배지 UI(홈 상단) + 레벨업 시 축하 배너. (2026-09-05, PR로 제출 — XP 데이터 모델 PR 위에 쌓은 브랜치)
- [x] 2단계·누적 성장: 뱃지 컬렉션 "명예의 전당" 화면(마이페이지/설정에서 진입). (2026-09-05, PR로 제출 — 레벨 배지 PR 위에 쌓은 브랜치)
- [x] 3단계·오늘 단위 쪼개기: 홈 화면 "오늘의 미션" — AI가 활성 목표별로 오늘 할 만한 작은 할 일을 1개씩 제안. (2026-09-05, PR로 제출)
- [x] 4단계·심리적 안전장치: 스트릭 프리즈(연속기록 보호권) — 하루를 놓쳐도 스트릭이 끊기지 않는 안전장치. (2026-09-05, PR로 제출)
- [x] 5단계·사회적 도파민: 피드/마니또에 원터치 응원 리액션(문구 작성 없이 한 번의 탭으로 응원). (2026-09-05, PR로 제출 — 마니또는 이미 구현돼 있어 피드만 보강)
- [x] 5단계·사회적 도파민: 내 기록에 반응·응원이 왔을 때 눈에 띄는 알림. (2026-09-05, PR로 제출)
- [x] 6단계·완주와 확산: 목표 최종 달성 시 트로피/인증서 이미지 생성(기존 공유 캔버스 인프라 재사용) + 공유. (2026-09-05, PR로 제출)
- [x] 6단계·완주와 확산: 주간 "위클리 리캡" 카드 자동 생성(스포티파이 랩드 스타일, 공유 캔버스 인프라 재사용). (2026-09-05, PR로 제출)

## 진행 중 / 대기

- [x] 실제 브라우저 푸시 알림: 지금은 탭이 열려있을 때만 알림이 옴(Notification API). Service Worker 기반 Web Push로 앱이 꺼져 있어도 체크인 시간에 알림 오도록 개선. (2026-09-05, PR로 제출 — Supabase 새 테이블·VAPID 키·Vercel Cron 등 사용자 설정 필요, PR 설명 참고)
- [x] 데이터 내보내기 개선: 설정의 내보내기가 현재 요약 위주인데, 체크인 전체 이력을 CSV/JSON으로 온전히 내려받을 수 있게 보강. (2026-09-05, PR로 제출)
- [ ] 접근성 점검 — 전역 기본값 대비는 여전히 사람 판단 필요, 고대비 모드로 부분 해결: 아이콘 전용 버튼 aria-label, 토글 스위치 키보드 접근성(role=switch 등)은 완료. WCAG AA 대비 계산 결과 `--ink-faint`(#9A9EB8)가 카드/페이퍼 배경 대비 2.3~2.6:1로 기준(4.5:1) 미달, red/gold/sage 브랜드 색을 텍스트로 직접 쓰는 곳도 배경(#F4F5FB) 대비 2~3:1로 미달. 전역 기본값 자체를 어둡게 하면 `--ink-soft`와 명도差이 거의 없어져 3단계 텍스트 위계·브랜드 톤이 눈에 띄게 달라지므로 여전히 사람 판단이 필요해 기본값은 그대로 둠. 대신 2026-09-07 PR로 `@media (prefers-contrast: more)` 고대비 모드 보정을 추가해, OS 접근성 설정을 켠 사용자는 기본 디자인 변경 없이 4.5:1 이상을 확보하도록 함(부분 해결). 전역 기본값 조정 여부는 여전히 다음 사람 판단으로 남김. (메모 2026-09-04, 갱신 2026-09-07)
- [x] 온보딩 개선: 첫 목표 생성 후 마니또·모임·공유 등 소통 기능을 자연스럽게 안내하는 짧은 투어/툴팁 추가(기존 온보딩 흐름 유지, 추가 단계만 삽입). (2026-09-05, PR로 제출)

## 컨트롤타워 후속 (2026-09-06 자율 사이클에서 발견 — 위에서부터 처리, 각 항목 4블록 1~2 포함)

- [x] **목표/기록 검색** (노션 성장 백로그 P0 실행순서 9). 기록 탭 검색 입력 + 순수 함수 `filterRecordsByQuery`로 본문·날짜·분야 부분일치 검색 구현. (2026-09-07, PR #61 병합 완료)
- [x] **페이월 캘린더 혜택 문구 정직화** (PR #54 리뷰어 지적). "캘린더 양방향 실시간 동기화 · 항상 최신 상태로"(index.html 1743)를 "목표·마일스톤 일정을 구글 캘린더로 보내기 · 항목별 버튼 한 번으로"로 교체(기존 정직한 안내문 재사용). (2026-09-08, PR로 제출)
- [x] **체크인 `category`가 Supabase에 저장·복원되지 않는 결함** (AUD-5 발견). `checkins` upsert·매핑에 category 추가 + 컬럼 없는 DB 폴백. (2026-09-07, PR #56 병합 완료 — 사용자가 `docs/sql/2026-09-06-checkins-category.sql`을 1회 실행해야 실제로 저장됨)
- [ ] **랜딩 부트 블록 함수 분리 + 게이트 순서 테스트** (AUD-8). 1~2블록: `landing_view` 하루 1회 게이트와 유입 캡처 순서는 현재 부트 IIFE 안에 있어 테스트로 고정할 수 없다(AUD-1 결함 유형) / `recordLanding(search, todayKey)` 같은 이름 있는 함수로 빼고 스모크에 "lv_day가 이미 오늘인 상태에서 ?utm_source 로드 → attrib 저장" 1건 + `document` 스텁. index.html PR(#48·#49·#51·#52·#54) 병합 후 착수(충돌 회피).
- [ ] **숨김 처리된 게시물·댓글의 REST 노출 차단** (PR #52 후속, AUD-7). 1~2블록: `hidden=true` 행도 `select` 정책(`auth.role()='authenticated'`)으로 API에서 읽힌다 / 정책에 `and hidden is not true` 추가 시 Realtime UPDATE 이벤트가 구독자에게 안 가 캐시가 낡는 부작용이 있으므로, (a) 정책 변경 + 클라이언트가 세션 중 1회 재조회, 또는 (b) 작성자 본인만 예외(`or auth.uid()=user_id`) 중 택일해 SQL 파일로. #52 병합·SQL 실행 후.
- [ ] **`docs/sql/*.sql` 문법 검사를 스모크에 추가** (AUD-7). 1~2블록: 새 테이블·RPC SQL이 어떤 DB에서도 실행되지 않은 채 PR이 열린다 / 외부 파서 없이 최소 검사(문장 끝 `;`·`$$` 짝·`create policy` 뒤 `on` 존재 등 정규식)라도 넣고, 가능하면 사용자 1회 작업으로 Supabase 스테이징 프로젝트를 만들어 병합 전 실행 검증 경로를 연다.

## 메타(엔지니어링 기반, 우선순위 높음)

## 완료됨

- [x] 그룹(모임) 랭킹·배지 다듬기: 이번 주 인증 랭킹 roster에 연속 인증일수 배지(🔥3일 이상) 표시 추가 (2026-09-04)
- [x] 주간/월간 리포트 화면: 기록 탭에 7일/30일 토글 가능한 SVG 추이 차트 + 카테고리별(TOPICS 6분야) 시간 분포 도넛 차트 추가. 기록에 분야(category) 선택 필드 신설 (2026-09-04)
- [x] 다크모드: 디자인 토큰(CSS 변수) 기준 prefers-color-scheme 다크 팔레트 추가, 배경/카드/텍스트에 var(--ink)를 배경색으로 겸용하던 15곳은 --chip으로 분리해 다크모드에서도 색상 뒤집힘 없이 유지 (2026-09-04)
- [x] PWA 설치 지원: manifest.json + 최소 서비스워커(sw.js) 추가, 아이콘 192/512 생성, 오프라인 시 안내 문구 화면 노출 (2026-09-04)
- [x] 최소 자동 스모크 테스트 스크립트 작성: `scripts/smoke-test.js` — `<script>` 문법 검증 + goalProgress/msCounts/resultPct/dDay/computeStreakDays/findSuggestionTarget/sanitizeSuggestions/applySuggestion/describeSuggestion 단위 테스트 20개 (2026-09-04)
- [x] 새 목표 AI 도우미(줄글 설명 → 마일스톤/할 일 템플릿 자동 생성 + 검증 + 리뷰 화면에서 수정 후 승인), 카테고리에 "직접 입력" 추가 (2026-09-05, 사용자 직접 요청·구현자 세션에서 즉시 반영)
- [x] 목표 상세 "목표 최종 결과" → "현재 종합상황"으로 개편: 목표·마일스톤·할 일이 바뀔 때마다 AI가 150~200자로 현재 상태와 다음 행동을 요약, 검증 후 노출 (2026-09-05, 사용자 직접 요청·구현자 세션에서 즉시 반영)
- [x] 프론트엔드 전면 리디자인, 목표 편집 UX 개선, 마니또 탭 (2026-09-04)
- [x] 하이브리드 다중 목표 대시보드, 기록+AI피드백 선택 공유 (2026-09-04)
- [x] 소통 탭 순서 변경, 공유 탭 플랫폼 맞춤 카드(실제 이미지 미리보기) (2026-09-04~05)
- [x] 기록 안내 문구, 기록 기반 목표 자동 업데이트 제안(상태/결과 메모), 구글 캘린더 연동 (2026-09-04~05)
- [x] 맞춤 피드백 봇 설정(대화형 생성 + 검증 + 수동 설정) (2026-09-05)
