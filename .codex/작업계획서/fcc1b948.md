# [작업계획서] #TASK-ES-053: 아바타 모달 미세조정 버튼(헤어스타일/표정/다른바디) 삭제 및 아바타 제작 한도 10회 확대

## 1. 배경 및 목적
- **상민님 직접 지시**: "이제 달라졌는데, 헤어스타일 변경, 표정 변경, 다른바디 입히기 삭제해. 그리고 아바타 제작 10회로 바꿔."
- **요구사항 분석**:
  1. AI 스튜디오의 최신 멀티모달 이미지 생성 모델(`gemini-3.1-flash-lite-image`)이 사용자 사진과 테마를 바탕으로 완결된 3등신 웹툰 일러스트를 직접 생성하므로, 기존 2D 캔버스 스티커 시절에 점/선을 미세조정하던 "헤어스타일 변경", "표정 변경", "다른 바디 입히기" 버튼은 불필요해졌으며 UX를 복잡하게 함. ➔ 해당 3종 버튼 및 관련 이벤트 핸들러 완전 삭제.
  2. 아바타 제작 가능 횟수 한도를 기존 3회에서 **10회**로 상향 조정하고 모달 안내문구, 버튼 텍스트, 잔여 횟수 계산 로직 전면 갱신.

## 2. 작업 범위
1. **`js/avatar-system.js`**:
   - `MAX_AVATAR_CHANGES = 10`으로 변경.
   - 모달 마크업에서 `btnRerollAvatarTheme`(다른 바디 입히기), `btnCycleHairStyle`(헤어스타일 변경), `btnCycleExpression`(표정 변경) 버튼 삭제.
   - 관련 이벤트 핸들러 및 불필요한 바인딩 코드 제거.
   - 모달 안내 텍스트(3회 ➔ 10회), 버튼 라벨(10회), 횟수 소진 경고(10회) 전면 갱신.
2. **`scripts/smoke-test.js`**:
   - #TASK-ES-053 검증 항목 추가:
     - `AvatarSystem.MAX_AVATAR_CHANGES === 10` 검증.
     - `getRemainingCrafts` 10회 기준 계산 검증 (1회 사용 시 잔여 9회, 10회 소진 시 잔여 0회).
     - `btnCycleHairStyle`, `btnCycleExpression`, `btnRerollAvatarTheme` 버튼 삭제 검증.
   - 기존 이전 태스크 assertions 조정.
3. **`index.html`**:
   - `avatar-system.js?v=20260913-es053` 캐시 버스팅 적용 (순증가 0줄 엄수).
4. **`docs/rules/TICKETS.md` & `dev_log.md`**:
   - 승인 티켓 등록 및 상세 변경사항 기록.

## 3. 검증 및 배포 계획
- `npm test` 222+개 테스트 100% 통과.
- `node scripts/essence-gate.js --pre-commit` 무결성 검증.
- Git commit, push, PR 생성 및 Squash Merge.
- Vercel 프로덕션 자동 배포 확인.
- Tri-Sync 100% 동기화 검증.

## 4. 진행 상태
- 상태: `완료`
- 담당: Antigravity
