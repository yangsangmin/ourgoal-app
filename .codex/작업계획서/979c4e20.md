# [#TASK-ES-124] 동반자 검색 2중 복원 및 가상유저 3인 AI 동반자 투명 뱃지 표기 작업계획서

## 목표 (본질)
동반자 검색 먹통(42501 권한/세션 오류 및 게스트 차단)을 Vercel 서버리스 1순위 파이프라인으로 100% 정상화하고, 가상유저 3인을 실 사용자로 오인하지 않도록 [🤖 AI 동반자] 뱃지 및 자가 치유 시스템을 배선한다.

## 체크리스트 (상한선: 4단계 로컬 메인 병합)
- [x] 1. 티켓 등록 (`docs/rules/TICKETS.md`에 `#TASK-ES-124`) · 예상 2분 · 완료 기준: TICKETS.md 내 티켓 등록 확인 (측정: 등록 및 4단계 완료 반영)
- [x] 2. Vercel 서버리스 검색 엔드포인트 구현 (`api/track.js` 내 `handleSearchUsers`) · 예상 5분 · 완료 기준: node 테스트로 닉네임 검색 API 응답 실측 (측정: handleSearchUsers 탑재 및 search_users 라우팅 완료)
- [x] 3. 클라이언트 2중 검색 파이프라인 및 게스트 검색 지원 (`js/team-invite-comm.js`) · 예상 7분 · 완료 기준: 검색 함수가 API 우선/RPC 폴백으로 호출되고 게스트도 검색 결과 렌더 확인 (측정: /api/track 1순위 -> sb.rpc 2순위 및 게스트 검색 허용 완결)
- [x] 4. 가상 유저 3인 [🤖 AI 동반자] 뱃지 및 자가 치유 (`js/team-invite-comm.js`) · 예상 8분 · 완료 기준: comp_ 및 가상 닉네임에 [🤖 AI 동반자] 뱃지 렌더 및 persistCompanions() 영속화 확인 (측정: isKnownAiCompanion 감지 헬퍼 탑재, 목록 및 프로필 뱃지 렌더, 자가 치유 영속화 완결)
- [x] 5. Supabase RPC 권한 완화 SQL 갱신 (`docs/sql/2026-09-16-search-users-rpc.sql`) · 예상 3분 · 완료 기준: SQL 문서 갱신 확인 (측정: DDL 파일 갱신 완료)
- [x] 6. 컴플라이언스 테스트 신설 및 `npm test` 261개+ 전수 통과 · 예상 5분 · 완료 기준: npm test 0 failure (측정: 263개 ALL PASS, 헌법 게이트 13/13 통과, Zero Dead Click 통과)
- [x] 7. 브라우저 실측 시뮬레이션 및 스크린샷 확보 · 예상 5분 · 완료 기준: 검색 렌더 및 AI 동반자 뱃지 실측 통과 (측정: scratch/verify_companion_ai_and_search.js 100% 통과)
- [x] 8. 로컬 main 병합 및 4단계 도달 (5A 프리뷰 배포 준비) · 예상 3분 · 완료 기준: 로컬 main 병합 완료 (측정: 2cf22c3 -> local main fast-forward 병합 완료, Tri-Sync 100% 무결성 확인)

## 막힐 지점 예상 (문제해결 8원칙 ⑧)
1. Vercel 서버리스 엔드포인트 로컬 환경 테스트 시:
   - 로컬 http-server 정적 서버에서는 `/api/track`이 405/404일 수 있으므로, 클라이언트에서 `fetch('/api/track')` 실패 시 즉시 `global.sb.rpc`로 폴백하도록 완벽한 예외 처리(try/catch)를 둔다.
2. 기존 사용자 계정에 이미 저장된 가상 유저 데이터:
   - 브라우저 로컬스토리지나 DB에 이미 `isAiBot: true` 없이 저장되어 있더라도, 렌더링 시점에 `isKnownAiCompanion()`이 감지하여 즉시 메모리 보정 및 백그라운드 DB 영속화를 수행해 100% 자가 치유한다.
3. `index.html` 라인 수 등 기존 불변식:
   - 이번 작업은 `js/team-invite-comm.js`와 `api/track.js`에 격리되어 진행되므로 `index.html` 라인 수에 영향을 주지 않는다.
