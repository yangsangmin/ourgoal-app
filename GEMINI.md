# GEMINI.md — 안티그래비티 진입 규칙 (포인터)

이 프로젝트의 규칙 원본은 저장소 루트의 `AGENTS.md` 하나다. 작업을 시작하기 전에 `AGENTS.md`, `docs/rules/OURGOAL_ABSOLUTE_INTEGRITY_RULES.md`, `docs/rules/ESSENCE_OURGOAL.md`를 먼저 읽고 그대로 따른다.

핵심 요약 (원본이 우선):
1. 나는 주장만 쓴다. 합격·완료는 GitHub 에서 도는 법정(court) 한 곳만 말한다. 내 세션에서 돌린 법정 출력은 예비 점검이며, 거기서 옮겨 적을 수 있는 것은 "막힌 것"뿐이다("이상 없음"은 옮겨 적지 않는다).
2. 못 잰 것은 확인 못 함이라고 쓴다(위반 아님). 수치·로그를 지어내거나 예시 문구를 붙이면 위반이다. 다만 PC 화면에서 눌러 보면 되는 종류를 재지 않고 확인 못 함으로 내면 돌려보내진다(헌법 제8조 제3항 5호의 닫힌 사유 목록에 해당할 때만 예외). 처음부터 법정이 잴 수 없는 것은 화면 동작 주장으로 내지 않고 처음부터 그 사유를 적어 확인 못 함으로 낸다 — 한 번 시험으로 청구한 주장은 그 PR 안에서는 그 시험을 통과해야만 풀린다. 한 변경에 주장 파일은 하나다.
3. 채점 기준(court, 헌법, 워크플로, 훅)은 고치지 않는다. 틀렸다고 보면 주장 파일의 법정 이의 칸(`objection`)에 재현과 함께 적는다 — 이의를 냈다고 재심 의무가 멈추지 않는다. 테스트 파일은 고칠 수 있지만 판정에는 쓰이지 않는다 — 법정은 고치기 전 시험지로 채점하고, 깨지는 기존 검사는 주장 파일의 retire 에 검사 제목별로 사유를 적어야 한다.
4. 보고의 첫 블록은 `node court/chat.js <PR>` 출력 그대로. 그 위에 아무것도 쓰지 않는다. 심사를 청구했으면 court 검사가 끝날 때까지 기다린 뒤 보고한다(30분 넘게 안 끝날 때만 그 사실을 첫 줄에 적고 멈춘다).
5. 돌려보내지면 멈추지 말고 고쳐서 재심한다. 한 PR 에서 돌려보냄이 3회 누적되거나(횟수는 GitHub 에 게시되는 판정이 센다) 승인선에 걸릴 때만 [결심 필요]. 안 되는 주장을 지우거나 철회·확인 못 함으로 바꾸거나 다른 폴더로 옮겨도 법정이 옛 시험을 다시 돌린다. 돌려보내진 작업을 새 PR 로 다시 낼 때에는 PR 본문 블록 1에 앞선 PR 번호를 적는다(횟수는 PR 단위로 세어진다). 푸시 전 예비 점검(pre-push)은 권고이며 판정이 아니다.
6. 로컬 main 에 병합하지 않는다. 브랜치를 올리고 초안 PR 을 연다. "1"은 법정 통과·확인 부족 커밋에만 유효하다(금고 변경·헌법 개정 승인을 요구하는 확인 부족에는 "1"이 적용되지 않는다). vercel --prod 는 쓰지 않는다.
7. **관제센터 저널(`C:\dev\command-center\journal.jsonl`)은 직접 열어 쓰지 않는다** — `fs.appendFileSync` 로 인라인 기록하면 prev·hash 체인이 끊긴다(실측: 2026-09-11~12 체인 밖 줄 3건, 전부 이 저장소 세션이 PR 병합/생성 뒤 직접 쓴 것). 기록은 반드시 `node C:/dev/command-center/lib/journal.js append <event> k=v ...` 한 문으로만 한다. 중첩 값(diff_stats 등)은 `diff_stats='{"files_changed":3}'` 처럼 JSON 문자열로 넘긴다 — 이게 안 된다고 인라인 스크립트로 해시를 직접 만들지 않는다(2026-09-19 실측: TASK-ES-189 task:done 이 그렇게 써져 해시 불일치 + 개행 누락).
8. **2중 8원칙**: 지시 → [1차 8원칙: REQ-*.md] → [2차 8원칙: PLAN-*.md] (원칙 번호 합체 금지, ⑥절차재검증 누락 금지, 본질·원인·중심·핵심 4대 요소 필수, 1줄 날림 금지). 무누락·무축약(`// ...`, 빈 stub 금지, 마크업+리스너+핸들러+피드백 4위1체 배선).
9. **티켓 게이트**: 본질 축(E1/E2/E3/INFRA/FIX)·체감 가설·근거 없이 코드를 만지지 않는다.
10. **브랜치**: main 직접 커밋 금지. `feat/` 또는 `fix/` 브랜치에서 작업한다.
11. **절대 금지 9대 행위(헌법 제4조) 및 승인선 8대 통제(필수 5 + 추가 3, 헌법 제5조)** 절대 준수, 작업마다 dev_log.md 기록 및 Tri-Sync(노션-옵시디언-관제센터) 동기화.

