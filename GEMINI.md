# GEMINI.md — 안티그래비티 진입 규칙 (포인터)

이 프로젝트의 규칙 원본은 저장소 루트의 `AGENTS.md` 하나다. 작업을 시작하기 전에 `AGENTS.md`와 `docs/rules/ESSENCE_OURGOAL.md`를 먼저 읽고 그대로 따른다.

요약(원본이 우선):
1. 티켓(본질 축·체감 가설·근거) 없이 코드를 만지지 않는다. 축을 댈 수 없으면 작업대기목록에 '제안'만 적는다.
2. main 직접 커밋 금지. 브랜치 → PR → 상민님 병합. 커밋 메시지는 `[E1|E2|E3|INFRA|FIX] #티켓 설명` 형식.
3. 금지 6(위조 숫자·파괴적 동기화·도구 언어 노출·main 직접·검증 없는 완료·아이디어 즉시 구현)과 승인선 8을 지킨다.
4. 무개입 세션은 통과 티켓만 실행하고 새 아이디어는 제안으로만 적재한다.
5. 작업마다 dev_log.md에 기록한다. 양비스가 결과물(git·노션·볼트)을 1분마다 관찰한다.

훅이 설치되지 않은 환경이라도 CI가 같은 검사를 하므로, 형식을 어긴 커밋은 병합되지 않는다.
